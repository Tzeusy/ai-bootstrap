interface:
  display_name: "Training Curriculum"
  short_description: "Build prerequisite curricula from repos"
  default_prompt: "Use $training-curriculum to inspect this repository, run the required 3 independent deep-dive prerequisite passes, generate or refresh curriculum/, and validate the result before finishing."
