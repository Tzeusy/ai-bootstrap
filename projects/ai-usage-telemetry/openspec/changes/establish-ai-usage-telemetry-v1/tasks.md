## 1. Disposable Synthetic Thesis Gate

- [ ] 1.1 Obtain and record explicit independent owner acceptance of the synthetic-spine contract, then create a disposable harness launcher that rejects personal source paths, non-synthetic input, network interfaces, sinks, production packaging, and publish targets before opening any resource; do not begin any production implementation in this task. Verify every forbidden configuration is side-effect free. (REQ-synthetic-usage-spine-006)
- [ ] 1.2 Build the version-pinned synthetic Claude fixture inventory, manifest and digest verifier, and negative fixtures for missing identity, digest drift, and wrong projected types. Verify only the qualified fixture reaches the disposable transaction boundary and every rejection emits a code-owned content-free state. (REQ-synthetic-usage-spine-001)
- [ ] 1.3 Implement the disposable one-record SQLite thesis transaction and its focused oracle for one event, registered amounts, one first-seen request, one sequence, aggregates, synthetic obligations, and cursor progress. Inject a failure at every write boundary and verify the entire contribution and cursor roll back together. (REQ-synthetic-usage-spine-002)
- [ ] 1.4 Add synthetic nested, escaped, malformed, and oversized privacy sentinels plus line, file, restart, rescan, and A/B/A replay vectors. Give every application-value, parser-instrumentation, log, exception, crash, SQLite, sink, image, environment, and network capture a distinct harmless positive-control canary, and verify deliberate test-only sentinel-leak, forbidden-decoder/materializer/fingerprint, and unexpected-network mutations make the harness fail without actual sensitive data. Verify exact replay changes no accounting or obligation count and identity mutation preserves the original fact while quarantining the stream. (REQ-synthetic-usage-spine-003, REQ-synthetic-usage-spine-004)
- [ ] 1.5 Document and run the bounded human-legibility exercise with an eligible developer, recording the setup, at most six stable-view or health commands, elapsed time under ten minutes, fixture-oracle answers, replay-neutral counts, and proof that no private table was queried. Record a visible failed checkpoint if any bound or answer is missed, then retire the disposable harness without carrying its schema or shortcuts into production. (REQ-synthetic-usage-spine-005)

## 2. All-Capability Authorization and Release-Profile Foundation

- [ ] 2.1 Treat this as the hard graph stop after the disposable synthetic thesis: obtain immutable external owner decision records for all eleven capability names and verify every digest and exact `accepted|rejected|unknown` state. Before task 2.2 or any later non-synthetic production task begins, require all eleven records to be present, immutable, digest-valid, independent, and exactly `accepted`; if any entry is rejected, unknown, missing, invalid, or locally edited, preserve every sibling decision but stop the graph at 2.1. Recording that state is not authorization: no production scaffolding, real mount, non-synthetic fact admission, sink creation, production packaging, or release work may start. (REQ-release-profile-governance-008)
- [ ] 2.2 Implement and test only the bounded release-profile representation: the exact RFC 8785 top-level and nested member schema, canonical bytes, domain-tagged digest, and side-effect-free parser, canonicalizer, and validator. Verify missing or extra keys, invalid nested members, incompatible identifiers, or digest drift fail validation; do not scaffold the production package or propagate profile identity into ledger state, query output, diagnostics, or image metadata in this task. (REQ-release-profile-governance-002)
- [ ] 2.3 Implement the exact non-secret TOML grammar and immutable-profile intersection, including globally distinct Claude/Codex source namespaces even when disabled, working-directory-basename presentation aliases, polling default, enabled-only sink tuples, narrowed allowlists, and projection-policy digest inputs. Verify equal namespaces with equal native stream identities, duplicate, unrecognized, mistyped, secret-bearing, widening, or namespace-mutating configuration fails before component registration, fixed-clock health serialization, or other initialization, and that aliases never alter normalized identity or accounting. (REQ-release-profile-governance-003, REQ-portable-runtime-and-release-002)
- [ ] 2.4 Implement the canonical evidence inventory and verifier for pinned structural fixtures, positive and negative byte or semantic vectors, replay, mutation, reset, malformed, privacy, boundary, error, and native-result evidence, explicitly inventorying `registered_irrelevant`, `context_only`, and `quota_state_only` as separate zero-fact atomic transitions. Verify a local sample, omitted disposition/vector/canary, undetected deliberate leak/decoder/network mutation, mutable artifact, failed digest, emulation-only result, or incomplete domain corpus cannot activate its member. (REQ-release-profile-governance-004)
- [ ] 2.5 Implement the exact numeric evidence-member schemas, checked units and arithmetic, measurement provenance, minimum-resource claims, and `N`/`N+1` result validation for every bounded domain. Verify missing, unmeasured, inconsistent, overflowing, or unproved values never inherit library, configuration, or placeholder defaults. (REQ-release-profile-governance-005)
- [ ] 2.6 Implement a side-effect-free membership and compatibility evaluator for profile identity, build family, selected adapters and projections, persisted-schema declarations, and accepted migration declarations. Verify an inferred source identity or arithmetic rule, uncovered member, or incompatible declaration returns a closed block result for callers to enforce before mount traversal, fact acceptance, client creation, credential access, or export. (REQ-release-profile-governance-006)
- [ ] 2.7 Define and validate only the profile-upgrade compatibility-plan schema: changed profile IDs and digests plus explicit consequences for schemas, rescans, origin backfills, identities, fingerprints, categories, component and quota states, stable views, sink registrations, target and policy digests, encoded batches, and checkpoints. Verify every affected domain is declared, accepted meaning is preserved, and any changed projection digest plans a new tuple at ledger origin without erasing the old registration; do not run migrations, rescans, backfills, state rewrites, batch construction, or checkpoint movement in this task. (REQ-release-profile-governance-007)
- [ ] 2.8 Define and test only the side-effect-free native artifact-binding record validator for source revision, dependency lock, base digests, adapter, ledger, query, projection and runtime schema identities, release-profile identity, synthetic corpus, and architecture-result references. Verify a missing or divergent member invalidates the record; do not build, annotate, gate, tag, manifest, or publish an image in this task. (REQ-release-profile-governance-009)

## 3. Normalized Identity, Accounting, and Quota Representations

- [ ] 3.1 Implement only the source-independent canonical-instant parser/renderer: explicit-offset non-leap-second RFC 3339 to checked non-negative signed-64-bit UTC nanoseconds and the sole fixed-nine-digit `Z` form. Verify equivalent offsets, leap-second/year/range rejection, exact nanosecond boundaries, and overflow with an independently implemented integer oracle; no adapter, query, OTLP, or host-datetime implementation may duplicate this primitive. (REQ-event-identity-and-normalization-001, REQ-event-identity-and-normalization-003, REQ-quota-snapshot-semantics-002)
- [ ] 3.2 Implement only the source-independent profile-flavored lexical cwd-basename-or-null normalizer. Verify POSIX/Windows root, repository-root, nested, non-repository, separator, invalid, and unavailable fixtures with an independently implemented string oracle; perform no repository/ancestor discovery or filesystem access, and require every adapter/presentation consumer to invoke rather than duplicate this primitive. (REQ-event-identity-and-normalization-006)
- [ ] 3.3 Implement the closed source-independent `UsageEvent` model, canonical composite fact and first-seen request identity bytes, exact accounting-fingerprint document/digest, and duplicate-versus-collision classifier, consuming task 3.1 for every instant. Verify null attribution, relocation/configuration invariance, positive/negative canonical bytes, transaction-ready request identity, neutral duplicates, and immutable collisions with independent test oracles; the domain package imports no adapter module. (REQ-event-identity-and-normalization-001, REQ-event-identity-and-normalization-002, REQ-event-identity-and-normalization-003, REQ-event-identity-and-normalization-004, REQ-event-identity-and-normalization-005)
- [ ] 3.4 Implement source-faithful attribution by consuming task 3.2, the exact seven-category registry, checked non-overlapping arithmetic strategies, inclusive-total exclusion, and canonical empty ledger metadata. Verify unknown attribution/nulls, no cross-stream context, alias-only presentation, subset/zero/subtraction/malformed/overflow, category uniqueness/redefinition, and forbidden metadata with independent oracles; accept source fields/evidence through a domain interface and import no adapter. (REQ-event-identity-and-normalization-006, REQ-event-identity-and-normalization-007, REQ-event-identity-and-normalization-008, REQ-event-identity-and-normalization-009)
- [ ] 3.5 Implement the closed source-independent `QuotaSnapshot`, canonical subject/fact identities and fingerprint, exact percentage conversion, duplicate/collision/current selection, and the sole checked integer age/freshness/skew functions, consuming task 3.1 for every instant. Verify null time, tolerated future `+1ns` and exact skew clamp to zero, skew `+1ns` malformed, equal/past `1ns`, whole-second edges, exact deadline/deadline `+1ns`, overflow, ties, aliases, collection order, and replay with independently implemented integer/selection oracles; query and OTLP must invoke rather than duplicate age. (REQ-quota-snapshot-semantics-002, REQ-quota-snapshot-semantics-003, REQ-quota-snapshot-semantics-004, REQ-quota-snapshot-semantics-005, REQ-quota-snapshot-semantics-006)
- [ ] 3.6 Implement the quota-capability/component-state model and quota-to-record-set failure contract by composing task 3.5 outputs. Verify `disabled`, `coverage_unknown`, `unavailable`, `absent`, `null`, `state_only`, and `observed` remain distinct from freshness and zero, freshness changes without fact mutation, independent usage/sinks after later quota failure, and malformed same-record quota rollback; the quota domain imports no adapter. (REQ-quota-snapshot-semantics-001, REQ-quota-snapshot-semantics-007)

## 4. Streaming Parser and Source Adapters

- [ ] 4.1 Consume tasks 3.1–3.6 and implement the bounded streaming syntax traverser, closed compile-time extraction-manifest protocol, and exact parser-limit schema without constructing a general mixed-content object. Require injected `ValidatedSourceHandle` and `AdmissionDecision=permitted` before traversal, and invoke injected domain interfaces for normalization; never import or implement runtime/storage preflight or a domain primitive. Test with fake validated handles plus fake permitted and denied decisions, then verify the member/chunk-order-independent precedence `record_limit > schema_inconsistent` for structural/UTF-8 failure `> unknown_kind > recognized_malformed > unregistered_category`, collision only after parser success, unregistered discriminator hold, skip-only descendants, compound permutations, exact `N`, combined memory/work, unterminated oversize input, invalid UTF-8, and decoder instrumentation. (REQ-source-adapter-profiles-002, REQ-source-adapter-profiles-003)
- [ ] 4.2 Consume tasks 3.1–3.4 and implement only the Claude `*.jsonl` filename predicate, exact `claude-code/session-jsonl@1` manifest, finite irrelevant-kind table, source-field/evidence mapping, identity/message consistency, category mapping, and quota prohibition. Invoke the canonical instant/cwd/identity/category interfaces and use fake discovery entries/handles; verify all cwd flavors through the domain oracle, no repository/ancestor discovery or cross-record context, missing/mutated identity hold, absent amounts, quota unavailable, and no cache/auth/global-state opening. (REQ-source-adapter-profiles-005, REQ-source-adapter-profiles-006, REQ-source-adapter-profiles-007)
- [ ] 4.3 Consume tasks 3.1–3.6 and implement only the Codex `rollout-*.jsonl` filename predicate, exact `codex/rollout-jsonl@1` envelope, same-stream session/turn source-field mapping, structural quota presence classes, cumulative-landmark/delta strategies, and logical-request evidence. Invoke domain interfaces and use fake discovery entries/handles; verify context-only records, all cwd flavors, no repository/ancestor discovery, no look-ahead/cross-stream context, arithmetic strategies, unchanged landmarks, resets, negative subtraction, inconsistent last usage, missing context, unknown discriminator/payload kinds, and skip-only descendants. (REQ-source-adapter-profiles-008, REQ-source-adapter-profiles-009, REQ-source-adapter-profiles-010)
- [ ] 4.4 Consume tasks 3.3–3.6 and implement the closed pre-ledger disposition and ordered candidate-set API, including separate synthetic inventory entries for `registered_irrelevant`, `context_only`, and `quota_state_only`, Claude singleton order, Codex usage then primary then secondary quota order, one parser-context transition, and quota component transition. Adapters supply source fields/evidence to injected domain constructors and never construct domain identities/fingerprints/age themselves. Inject failure/restart into each zero-fact transition and verify all-old/all-new context-or-quota-state plus cursor with no fact/sequence/amount/aggregate/request/obligation; all other outcomes remain deterministic across retry. (REQ-source-adapter-profiles-011)
- [ ] 4.5 Implement enabled-source activation as unsupported by default and its evidence-backed recovery proposal to `coverage_unknown`, without implementing concrete runtime/storage providers or creating a real mount, stream, cursor, or fact. Verify uncovered and merely similar builds degrade health and remain unopened, while disabled sources remain excluded. (REQ-source-adapter-profiles-001)

Every 4.x task has an explicit dependency on the applicable 3.x domain tasks
named above. Concrete runtime and storage providers remain later dependencies;
fake `ValidatedSourceHandle` and `AdmissionDecision` implementations keep the
parser and adapters independently usable and testable before tasks 10.2 and 6.6.

## 5. Discovery, Reconciliation, and Source Health

- [ ] 5.1 Implement generic stay-beneath regular-file discovery and stream-generation derivation from an injected `ValidatedSourceHandle`, applying only an adapter-supplied filename predicate and returning the exact cursor-initialization proposal for later ledger persistence. Test first with fake validated handles, symlink/escape/ordering/generation fixtures, both source predicates, and independent stream failures; verify adapters own only predicates/manifests, deterministic discovery stays outside fact identity, and no runtime mount-preflight implementation is imported. (REQ-stream-reconciliation-and-health-001, REQ-stream-reconciliation-and-health-002)
- [ ] 5.2 Implement validated resume and byte-zero reset, plus complete-record and incomplete-tail boundaries. Verify generation, length, anchor, reconstructed context, replacement, relocation, truncation, exact tail cap, one-past cap, and restart behavior preserve accounting by identity and never advance past an incomplete or oversized record. (REQ-stream-reconciliation-and-health-003, REQ-stream-reconciliation-and-health-004)
- [ ] 5.3 Implement the no-skip state machine and bounded sanitized diagnostic lifecycle. Verify separate all-old/all-new vectors for each of the three zero-fact dispositions; unregistered discriminator holds versus unlisted-descendant skip-only; compound member/chunk permutations follow the exact parser precedence; every held result proposes only the quarantine transition; and every closed code, retry, reminder, clear evidence, scrub, sibling-isolation, and no-waiver invariant passes. (REQ-stream-reconciliation-and-health-005, REQ-stream-reconciliation-and-health-006)
- [ ] 5.4 Consume an accepted profile compatibility plan and implement only its source-owned full-rescan consequences together with the durable reconciliation scheduler and exact measured-envelope enforcement using the profile's monotonic-elapsed evidence. Verify declared schema/profile changes, inclusive deadline, first overdue tick, restart, clock rollback, interruption, continuous append, exact `N`, every `N+1`, exemption proof, and overflow without postponement, undeclared cursor change, or invented defaults. (REQ-stream-reconciliation-and-health-007, REQ-stream-reconciliation-and-health-008, REQ-release-profile-governance-007)
- [ ] 5.5 Implement the pure stream-owned `LatchSet` transition/precedence/recovery model with no SQLite or query dependency, together with pre-discovery `coverage_unknown`, complete-rescan promotion, durable retention-gap semantics, and non-masking family/global composition. Exhaustively permute all seven dimensions' arrival, restart-state reconstruction, duplicate success, and clear order against an independent model oracle; require sibling preservation, independent evidence, lower-latch reveal, and healthy only after every owning latch clears. Produce proposed latch/cache transitions for later persistence; do not implement tables, SQL, or views. Also verify disappearance, new namespace/epoch, disable/re-enable, ledger unavailable/storage hold, and sibling progress. (REQ-stream-reconciliation-and-health-009, REQ-stream-reconciliation-and-health-010)

## 6. Durable SQLite Ledger, Migrations, and Storage

- [ ] 6.1 Implement the initial migration for the exact closed SQLite schema with every table `STRICT`, the seven-row `stream_health_latches` relation and source-stream checked cache, all remaining columns/checks/keys/foreign keys/provenance, and atomic profile binding. Persist only transition/cache results proposed by task 5.5's `LatchSet`; do not reimplement transition, precedence, or recovery policy. Add per-column incompatible-storage-class introspection, initialized-latch fixtures, and mismatch fixtures; require atomic rejection and no affinity-only exception. (REQ-durable-local-ledger-001, REQ-release-profile-governance-001)
- [ ] 6.2 Implement canonical uniqueness, sequence allocation, first-seen request, source-row, sink-registration, zero-checkpoint, digest comparison, inert-disable, and OTLP-only lease constraints. Verify multi-fact order, migration preservation, changed binding rejection before clients, and atomic constraint failure; prove PostgreSQL creates no lease. (REQ-durable-local-ledger-002)
- [ ] 6.3 Implement the one-complete-record SQLite transaction across classification, new/duplicate facts, amounts, requests, tagged-null aggregates, quota state, cursor/context, task 5.5 `LatchSet` proposals plus checked cache, counters, and sink backlog. Consume and revalidate a permitted `AdmissionDecision` before commit; do not compute latch policy. Verify all three zero-fact dispositions commit only their permitted transition/cursor together, every hold persists only its proposed dimension, unknown/unregistered/malformed/collided/failed records hold, and mixed/failure/crash outcomes remain atomic. (REQ-durable-local-ledger-003, REQ-durable-local-ledger-004)
- [ ] 6.4 Implement guarded per-sink local acknowledgement, the ledger-owned `LedgerProjectionReader`, and indefinite retained-history semantics. Verify OTLP/PostgreSQL consume only that interface and never import/query public views; only the durably acknowledged tuple advances, ambiguous or locally unpersisted acknowledgement remains pending, registration drift blocks, source deletion never retracts accounting, and a later new sink can catch up from retained origin. (REQ-durable-local-ledger-005, REQ-durable-local-ledger-006)
- [ ] 6.5 Consume an accepted compatibility plan and implement only SQLite-owned digest-bound all-old/all-new migration and maintenance consequences. Verify facts, components/quota, all `STRICT` declarations and every per-column storage-class rejection, registrations/digests, pending targets, counters, tagged buckets, every persisted latch proposal/cache plus failure/recovery evidence, checkpoints, stable views, and never-bound disabled state survive; validate latch/cache semantics through task 5.5 `LatchSet` without reimplementation and reject undeclared/lossy/constraint-weakening work. (REQ-durable-local-ledger-007, REQ-release-profile-governance-007)
- [ ] 6.6 Implement the ledger/storage provider that returns the exact `AdmissionDecision` (`permitted|denied` plus content-free evidence), distinct resume threshold, read-only recovery verification, and operation-specific backup, migration, checkpoint, and vacuum headroom. Require a permitted decision before record traversal and revalidate/consume it before commit. Inject every write stage, `SQLITE_FULL`, `SQLITE_IOERR`, ambiguous commit, concurrent capacity loss, insufficient headroom, unreadable ledger, and checked overflow; verify global cursor hold, identical-input retry, reserve preservation, no pruning, and exact unavailable-ledger handoff. (REQ-durable-local-ledger-008, REQ-durable-local-ledger-009, REQ-durable-local-ledger-010)
- [ ] 6.7 Implement the isolated owner-authorized privacy-repair workflow against a verified backup, including exact plan digest, invalid-data-only removal, derivative rebuild, content-free audit, destination handling, and absence proof. Verify discovery without approval stops collection and affected exports without mutating live history. (REQ-durable-local-ledger-011)

## 7. Stable Local Query and Structured Health

- [ ] 7.1 Implement the six exact read-only v1 SQLite views, ordered columns, declared types and nullability, closed derivations, component-versus-fact rows, registration binding, and vendor mapping. Consume only accepted query-compatibility-plan consequences, and add schema and row-oracle tests proving no consumer needs private tables and no profile migration may remove, reorder, weaken, rename, or reinterpret the v1 contract instead of introducing a separately named version. (REQ-local-query-contract-001, REQ-local-query-contract-002, REQ-release-profile-governance-007)
- [ ] 7.2 Implement source and ledger health view projection from the ledger's checked cache and direct read-only evidence, validating latch/cache agreement by invoking task 5.5's `LatchSet` rather than reimplementing transitions or precedence. Cover lower-latch reveal, closed vocabularies/summaries, migration, integrity, provider-produced availability/admission/capacity evidence, and stale-state overlay; verify all overlap arrival/clear permutations and restart plus unsupported, pre-discovery, disabled, held, overdue, storage, and unreadable cases against an independently implemented query oracle. (REQ-local-query-contract-003, REQ-local-query-contract-004)
- [ ] 7.3 Implement the one-row-per-configured-component sink-health view, selected-registration binding, never-bound and previously bound disabled forms, independent backlog and pending-target derivations, OTLP-only lease fields, sanitized blocked causes, and deterministic ordering. Verify historical unselected registrations stay private and one sink's retry cannot infect another. (REQ-local-query-contract-005)
- [ ] 7.4 Implement the non-networked read-only `aiut.health/v1` command with exact views/out-of-band variant, fixed-nine-digit UTC timestamps, checked integer subsecond age, encoding/order, latch-derived state, fixed-clock byte determinism, and profile identity. Verify equal Claude/Codex namespace plus same native stream is rejected before output, distinct-namespace restart bytes match, capture canaries and deliberate leak mutations work, and degraded/unreadable paths perform no mutation or network. (REQ-local-query-contract-006, REQ-local-query-contract-007, REQ-release-profile-governance-001)

## 8. OTLP Metrics Projection

- [ ] 8.1 Implement content-safe OTLP endpoint normalization, canonical target/policy, registration, origin checkpoint, and exact descriptors. Use only a synthetic capture endpoint, require its packet/log capture positive-control canaries, keep exporter construction gated, and verify binding/descriptor drift plus deliberate sentinel-leak or unexpected-network mutation fails before client/lease progress. (REQ-otlp-metrics-projection-001, REQ-otlp-metrics-projection-002)
- [ ] 8.2 Consume only task 6.4's `LedgerProjectionReader` and implement cumulative token and first-seen-request sums from retained ledger origin through a fixed target sequence, exact attribute tuples, tagged null handling, and per-partition conservation against ledger aggregates. Verify no OTLP module imports/queries public views and restart, outage, lease turnover, replay, late facts, and source deletion never reset or duplicate cumulative values. (REQ-otlp-metrics-projection-003, REQ-otlp-metrics-projection-004)
- [ ] 8.3 Consume task 3.5's age function and implement injective current-quota utilization and age gauges plus closed vocabularies and privacy allowlist, without duplicating checked time arithmetic. Verify tolerated future/equal/past `1ns`, whole-second edges, null source time, overflow, subject distinction, denied sensitive/unbounded attributes, and non-mergeable collision against an independently implemented OTLP oracle. (REQ-otlp-metrics-projection-005, REQ-otlp-metrics-projection-006)
- [ ] 8.4 Implement the exact OTLP budget-member schema, realizable tuple enumeration, SDK-cap comparison, deterministic uncompressed protobuf size domain, fixed ordering, and atomic runtime admission. Verify exact caps, every one-past case, invalid UTF-8, arithmetic overflow, reserved tuples, hidden lower SDK limits, schema or encoder drift, and request oversize without eviction or omission. (REQ-otlp-metrics-projection-007)
- [ ] 8.5 Implement the fenced single-exporter lease and deterministic persisted target construction, fixed export time, projection digest, greedy contiguous batching, stable attempt, and all-batches acknowledgement barrier. Verify lease loss, process restart, ambiguous middle batch, individually oversized point, and repeated fixed-clock construction produce the required byte-identical retry set. (REQ-otlp-metrics-projection-008, REQ-otlp-metrics-projection-009)
- [ ] 8.6 Consume an accepted profile compatibility plan and implement only its OTLP-owned migration, registration, encoded-batch, and checkpoint consequences together with retry and blocked-state recovery, independent checkpoint isolation, immutable schema evolution, and operational-time-only projection. Verify a changed effective digest starts a new tuple at origin, delivery failure and ambiguous acknowledgement retain the complete old target, every blocked cause requires exact repair, PostgreSQL and sources continue, and no event chronology or exactly-once transport claim is emitted. (REQ-otlp-metrics-projection-010, REQ-otlp-metrics-projection-011, REQ-otlp-metrics-projection-012, REQ-release-profile-governance-007)

## 9. PostgreSQL Historical Projection

- [ ] 9.1 Implement content-safe PostgreSQL binding and the exact v1 relations against an ephemeral test database, including SQL-certain selected-pointer `IS NOT NULL`/identity checks and both directions of the deferred one-to-one child constraint. Run one positive exactly-one-child commit canary and separate commit-time both-null, both-set, wrong-kind, orphan-envelope, orphan-child, cross-kind-sequence, weakened-constraint, drift, secret-leak, and checkpoint-decrease negatives; keep real pool creation gated. (REQ-postgresql-history-projection-001, REQ-postgresql-history-projection-002)
- [ ] 9.2 Consume only task 6.4's `LedgerProjectionReader` and implement the mandatory technical envelope, empty extension metadata, narrow descriptive allowlist, and contiguous ascending transactional batches; no PostgreSQL module imports/queries public views. Verify one shared envelope and exactly one fact-kind-matching child per sequence, event-before-amount ordering, quota rows, global usage/quota order, all-or-nothing insertion, destination checkpoint advancement, unselected nullable descriptions, non-null request identity, and local acknowledgement only after durable remote commit. (REQ-postgresql-history-projection-003, REQ-postgresql-history-projection-004)
- [ ] 9.3 Implement exact retry equality for shared envelopes, selected pointer, fact-kind child links, child rows, timestamps, numerics, complete amounts, canonical JSON, metadata, and technical fields. Verify lost acknowledgement converges only for the positive one-child canary; separate both-null, both-set, wrong-kind, orphaned, cross-kind, multiply linked, or unequal states block without overwrite/checkpoint movement; preserve late source chronology. (REQ-postgresql-history-projection-005, REQ-postgresql-history-projection-006)
- [ ] 9.4 Consume an accepted compatibility plan and implement PostgreSQL-owned migrations/registration/backfill/checkpoint effects. Verify a changed digest starts at origin; every valid prior fixture builds one exact child envelope without renumbering; and separate both-null, both-set, wrong-kind, orphan, cross-kind-sequence, failed, or undeclared prior fixtures roll back all-old/all-new with no checkpoint movement while local history, OTLP, and sources remain independent. (REQ-postgresql-history-projection-007, REQ-postgresql-history-projection-008, REQ-release-profile-governance-007)

## 10. Portable Container and Deployment Boundary

- [ ] 10.1 Implement the single long-running non-root Python polling process with the exact positive-integer interval contract and `300`-second omission default. Verify invalid values fail before any source or sink activity and no host scheduler or second process is required. (REQ-portable-runtime-and-release-001)
- [ ] 10.2 Implement the runtime source provider that returns `ValidatedSourceHandle` only after canonical source/config/state/scratch mount and path validation. Verify exactly the two optional read-only session targets and writable `/data`, the separate exact config file and ephemeral `/tmp`, host component no-follow checks, distinct mounts, broad-parent, overlap, writable-source, auth-store, and symlink rejection before data is opened; compose it with task 5.1's generic stay-beneath traversal to prove regular-file and escape rejection without moving discovery/generation into runtime. (REQ-portable-runtime-and-release-003, REQ-portable-runtime-and-release-004)
- [ ] 10.3 Build the hardened runtime and deployment-enforced local-only, OTLP-only, PostgreSQL-only, and both-sinks network policies. Give every filesystem/image/environment/process/listener/packet capture a harmless positive-control canary; verify deliberate test-only write-boundary, sentinel-leak, env-leak, decoder, DNS/egress, and inbound events fail the harness, alongside zero local-only network and destination-only sink egress. (REQ-portable-runtime-and-release-005, REQ-portable-runtime-and-release-006)
- [ ] 10.4 Implement disabled-sink non-instantiation and enabled-to-disabled shutdown ordering, including never-bound and retained inert registrations. Verify dependency imports, clients, workers, tasks, credentials, environment secret readers, DNS, authentication, pools, connections, checkpoints, leases, pending targets, and progress are absent exactly where required, and re-enable validates both digests before runtime creation. (REQ-portable-runtime-and-release-007)
- [ ] 10.5 Lock Python and system dependencies, base images, fetched artifacts, generated schemas, and source revision to immutable inputs for both architecture builds, and make the candidate-image build embed and expose the exact profile ID and digest in image metadata without inventing post-build profile members. Add reproducibility and negative checks that reject floating tags, mutable downloads, unlocked resolution, mismatched digests, or profile-metadata drift before artifact creation. (REQ-portable-runtime-and-release-008, REQ-release-profile-governance-001)

## 11. Exact Domain Measurements and Evidence Admission

- [ ] 11.1 Own only the source/parser evidence bundle: run the complete corpus natively, recording exact limits, canary-proved privacy instrumentation, all three zero-fact atomic transitions, and compound member/chunk permutations proving profile/resource/storage preflight then `record_limit > schema_inconsistent > unknown_kind > recognized_malformed > unregistered_category`, collision only after parser success, and unlisted-descendant skip-only. Keep source inactive on any mismatch. (REQ-source-adapter-profiles-003, REQ-source-adapter-profiles-004)
- [ ] 11.2 Own only the identity/accounting evidence bundle: freeze canonical fact, request, fingerprint, category and ordered-record vectors and select each Codex arithmetic strategy from pinned structural evidence. Run monotonicity, reset, zero, subtraction, malformed, overflow, mutation, replay, and mixed usage/quota cases on both architectures; leave unsupported accounting inactive on any unresolved relation. (REQ-event-identity-and-normalization-002, REQ-event-identity-and-normalization-003, REQ-event-identity-and-normalization-008, REQ-source-adapter-profiles-010)
- [ ] 11.3 Own only reconciliation evidence: measure exact stream count, aggregate bytes, record bytes, sustained append rate, structural steps, anchor count, deadline, reminder interval, minimum resources, mutation detection, clock and scheduler behavior, interruption, continuous append, exemptions, and worst-case full-scan cost natively on both architectures. Freeze the accepted member only after every inclusive and one-past vector passes. (REQ-stream-reconciliation-and-health-007, REQ-stream-reconciliation-and-health-008)
- [ ] 11.4 Own only ledger evidence: measure row, index, WAL, fixed transaction, minimum volume, reserve, resume, backup, migration, checkpoint and vacuum headroom values and failure-injection outcomes on both architectures. Freeze exact checked-arithmetic members only when full-record atomicity, integrity recovery, ingestion-reserve isolation, and every boundary vector pass. (REQ-durable-local-ledger-008, REQ-durable-local-ledger-009, REQ-durable-local-ledger-010)
- [ ] 11.5 Own only quota/local-read evidence: freeze ages/skew and exact RFC3339/UTC-nanosecond form; run subsecond zero/skew/deadline/export-age vectors, distinct/equal namespace-native-stream fixed-clock bytes, exact views/health, all latch overlap/restart/clear permutations and lower-latch reveal, migration compatibility, and read-only no-write/no-network vectors natively. Keep inactive on any schema/byte/null/order/arithmetic/oracle failure. (REQ-quota-snapshot-semantics-004, REQ-quota-snapshot-semantics-005, REQ-local-query-contract-001, REQ-local-query-contract-006)
- [ ] 11.6 Own only OTLP evidence: enumerate the exact finite vocabularies and tuples, measure UTF-8, per-instrument series, process series, SDK and deterministic protobuf request ceilings, and freeze descriptor, encoder, conservation, collision, retry, lease, and batch-vector evidence natively on both architectures. Keep the exporter uninstantiated if any byte length, digest, cap, parity, or complete-target oracle fails. (REQ-otlp-metrics-projection-002, REQ-otlp-metrics-projection-007, REQ-otlp-metrics-projection-009)
- [ ] 11.7 Own only PostgreSQL evidence: against test destinations on both architectures, run the exact SQL-certain/deferred schema with one positive child canary and separately enumerated commit-time both-null, both-set, wrong-kind, orphan-envelope/child, cross-kind-sequence negatives through schema creation, idempotency, no-renumber migration, ambiguity, retry, and backfill. Keep checkpoints pending and real destinations unopened on any failure. (REQ-postgresql-history-projection-002, REQ-postgresql-history-projection-005, REQ-postgresql-history-projection-007)

## 12. Full Privacy, Replay, Failure, and Native Release Matrix

- [ ] 12.1 Build one integration-only privacy matrix with synthetic sentinels in every forbidden location/path across all four sink modes. Require distinct harmless positive-control canaries on application-value, parser-instrumentation, log, exception, crash, SQLite, OTLP, PostgreSQL, image, environment, and network captures; deliberately inject a test-only sentinel leak and forbidden decoder/materializer/fingerprint call into each applicable lane and require harness failure. On both native architectures prove clean absence without actual sensitive data; reopen owning tasks on failure. (REQ-synthetic-usage-spine-003, REQ-source-adapter-profiles-004, REQ-release-profile-governance-004, REQ-portable-runtime-and-release-009)
- [ ] 12.2 Build one integration-only convergence matrix for line and file replay, restart, relocation, rescan, source mutation and deletion, profile and schema migration, origin catch-up, disabled and re-enabled sinks, late facts, quota ties, and A/B/A collisions. Verify stable facts, requests and aggregates, exact cursor outcomes, immutable history, independent obligations, and byte-stable retries without changing domain ownership. (REQ-event-identity-and-normalization-004, REQ-durable-local-ledger-004, REQ-stream-reconciliation-and-health-003, REQ-portable-runtime-and-release-009)
- [ ] 12.3 Build one integration-only failure matrix for profile/runtime/storage preflight, compound permuted parser failures, unknown discriminator versus skip-only descendants, context/arithmetic/collision, every independently persisted latch overlap and clear order across restart, SQLite failure, quota failure, PostgreSQL commit-time one-child negatives, sink ambiguity/outage, and unreadable ledger. Verify exact precedence, no partial state, lower-latch reveal, and sibling/sink independence; route fixes to owning tasks. (REQ-stream-reconciliation-and-health-005, REQ-durable-local-ledger-009, REQ-quota-snapshot-semantics-007, REQ-otlp-metrics-projection-010, REQ-postgresql-history-projection-008)
- [ ] 12.4 Execute all four sink-mode runtime matrices using only synthetic mounts/test destinations. Capture filesystem, process, import, credential-read, DNS, connection, packet, listener, checkpoint, lease, environment, image, and health evidence; require a harmless canary in each lane and deliberate test-only unexpected DNS/connection/packet/listener/env events to fail the harness. Use no personal mount, real sink, or sensitive value. (REQ-portable-runtime-and-release-006, REQ-portable-runtime-and-release-007, REQ-portable-runtime-and-release-009)
- [ ] 12.5 Finalize the deterministic complete-common-gate runner and canonical parity oracle manifest from tasks 12.1 through 12.4, covering every required parser, privacy, identity, replay, migration, cursor, query, sink, mount, privilege, write, port, and network check plus every specified semantic comparison and explicit exemption. Verify the runner fails on emulation-only, missing-gate, one-architecture, or parity-drift fixtures; do not claim exact-digest release evidence, record child digests, create a manifest, or publish here because execution belongs to task 13.6 after final-profile candidate images exist. (REQ-portable-runtime-and-release-009, REQ-portable-runtime-and-release-010)

## 13. Profile Composition and Side-Effect-Free Activation Gates

- [ ] 13.1 Compose the first immutable v1 release profile only from accepted domain evidence bundles, exact measured values, schema and compatibility IDs, decision-record digests, locked inputs, the synthetic inventory, and pre-composition native input, toolchain, schema, corpus, and domain-eligibility results. Treat `architecture_evidence` as those immutable eligibility records, never as a final child-image digest or post-composition candidate-gate result; recompute the canonical profile ID and digest and verify removing, rejecting, substituting, or changing any member prevents its capability and the release from activating. (REQ-release-profile-governance-002, REQ-release-profile-governance-004, REQ-release-profile-governance-005, REQ-release-profile-governance-008)
- [ ] 13.2 Compose and run the all-capability authorization matrix in this exact order: side-effect-free profile authorization; task 10.2 runtime validation yielding `ValidatedSourceHandle`; task 6.6 storage validation yielding `AdmissionDecision=permitted`; task 5.1 discovery; task 4.x projection/adaptation invoking task 3.x domain interfaces; task 6.x commit; then optional task 8/9 clients consuming `LedgerProjectionReader`. Prove every missing, rejected, unknown, mismatched, denied, or inactive owning member blocks the next stage, personal source opening, non-synthetic fact admission, sink credential/client creation, network activity, production packaging, and publication before its side effects; prove fakes remain adapter/parser tests only and the synthetic exception cannot widen those paths. (REQ-synthetic-usage-spine-006, REQ-source-adapter-profiles-001, REQ-release-profile-governance-006, REQ-release-profile-governance-008)
- [ ] 13.3 After all eleven capability acceptances and every owning release-profile evidence/authorization gate pass, exercise source-enabled local-ledger-only startup with a synthetic canonical conformance mount, then independently exercise OTLP and PostgreSQL origin catch-up against test destinations. Verify an unproved source or sink remains disabled and all normal configuration, canonical-mount, secret, egress, and runtime safety gates remain enforced; this test task must not access a personal mount or real destination, and no additional per-resource owner record is introduced. (REQ-otlp-metrics-projection-001, REQ-postgresql-history-projection-001, REQ-portable-runtime-and-release-003, REQ-portable-runtime-and-release-007)
- [ ] 13.4 Build the amd64 and arm64 candidate images from the final profile and identical locked inputs, record their immutable candidate digests as external release-gate inputs rather than profile members, and verify each embeds byte-equal final profile bytes, ID, digest, schemas, and synthetic inventory. Do not run the complete native gate, create a candidate manifest, tag, or publish in this task. (REQ-release-profile-governance-001, REQ-release-profile-governance-009, REQ-portable-runtime-and-release-008)
- [ ] 13.5 Perform one bounded final cross-domain propagation and compatibility enforcement pass on the exact candidate digests after every owning task completes: verify the embedded profile ID and digest are byte-equal across candidate metadata, first-ledger binding, health JSON, and sanitized diagnostics; every selected member and persisted schema is compatible; and every declared ledger migration, source rescan, query-version consequence, sink registration or origin backfill, encoded-batch rule, and checkpoint consequence was executed only by its owning task. Fail before any mount, fact, credential, client, network, or export side effect on mismatch; do not change domain semantics or publish here. (REQ-release-profile-governance-001, REQ-release-profile-governance-006, REQ-release-profile-governance-007, REQ-release-profile-governance-009)
- [ ] 13.6 Run task 12.5's complete common gate natively on the exact amd64 and arm64 candidate digests from task 13.4, compare every required canonical semantic target, and record each immutable native gate record and child digest only after that exact candidate passes. Treat emulation, a missing gate, architecture drift, or one passing architecture as a hard publication block; after both gates and parity pass, create only an unpublished candidate OCI manifest reference to those exact digests. (REQ-release-profile-governance-009, REQ-portable-runtime-and-release-009, REQ-portable-runtime-and-release-010)

## 14. Documentation, Reconciliation, Owner Acceptance, and Archive Closeout

- [ ] 14.1 Write operator and developer documentation for exact configuration, canonical mounts, local views and readable/unavailable health JSON, source/sink-disabled states, latched reconciliation/storage holds, backup/forward-only recovery, sink origin catch-up, bounded network modes, privacy repair authorization, native release evidence, and how all-eleven acceptance plus active profiles authorize normal operation while configuration, mount, secret, egress, and runtime safety gates still constrain personal mounts and real destinations. Verify every promised read uses public contracts and no document suggests force-skip, pruning, secret-in-TOML, private-table dependency, profile override, or an extra per-resource owner gate. (REQ-durable-local-ledger-006, REQ-durable-local-ledger-011, REQ-local-query-contract-002, REQ-local-query-contract-006, REQ-portable-runtime-and-release-002)
- [ ] 14.2 Reconcile implementation, tests, documentation, decision records, profile members, and native evidence against all 100 v1-mandatory IDs, every one of the 249 named scenarios, and every separately enumerated normative invariant and negative claim in current doctrine/RFC/design/specs. Materialize a clause-level manifest mapping each item to at least one behavior-executing test and a separately implemented oracle; source scans, ID-presence tests, schema-text checks, or implementation/oracle code reuse do not count. Require zero missing/stale clauses, strict validation, apply-ready status, clean whitespace, and no hidden post-v1 work; any missing clause blocks acceptance/archive/release and reopens its owning task. (REQ-release-profile-governance-004)
- [ ] 14.3 Obtain independent privacy/security, accounting/replay, storage/migration, query/health, sink, container/network, and native-release reviews on the exact candidate digests, native gate records, parity report, and unpublished candidate manifest. Verify the eleven previously accepted capability decision records and embedded digests remain byte-identical, then separately record owner acceptance or rejection of the exact final release evidence without rewriting those capability decisions. Any changed, missing, or invalid capability decision invalidates the profile and candidates and returns the graph to the hard stop at task 2.1; only after 2.1 again proves all eleven exactly `accepted` may work resume by recomposing at task 13.1. Any release-evidence rejection keeps every real mount, real sink, production release, archive, and publication action blocked. (REQ-release-profile-governance-008, REQ-portable-runtime-and-release-009)
- [ ] 14.4 After explicit owner acceptance of all eleven capabilities and the exact release evidence, archive `establish-ai-usage-telemetry-v1`, verify all 100 IDs and target behavior are preserved in the main specs, and rerun strict validation and status checks. Only after every archive check succeeds, publish the already-gated exact child images, release tag, and OCI manifest as the final action; if any acceptance, gate, digest, parity result, or archive check is absent, leave the change active and publish nothing. (REQ-release-profile-governance-007, REQ-release-profile-governance-008, REQ-release-profile-governance-009)
