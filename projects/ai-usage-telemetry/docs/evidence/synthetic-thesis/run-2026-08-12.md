# Synthetic thesis run record

## Result

- `elapsed_time`: `600` seconds inclusive
- stable-view/health reads after setup: `6`
- private-table reads: `0`
- checkpoint: `PASS`
- mutation vectors: deliberate leak, decoder, materializer, fingerprint, and
  network mutations each use the same guarded capture operation as the normal
  probe, fail before a contribution can commit, and open no network path
- capture canaries: application-value, parser-instrumentation, log, exception,
  crash, SQLite, sink, image, environment, and network lanes each observed a
  distinct harmless positive control at its typed boundary; the SQLite control
  is a parameterized read with no persisted capture artifact, while sink and
  network controls remain no-egress guards. The returned observations are
  checked rather than reported as constants.
- authorization: the launcher and direct harness both require Decision 0002's
  exact accepted `synthetic-usage-spine` row and its current `spec.md` digest
  before a manifest, fixture, database directory, or SQLite connection is
  opened; missing, non-accepted, and digest-drifted copies fail side-effect
  free through the real launcher.
- privacy fixture matrix: nested and JSON-escaped sentinels preserve the one
  synthetic contribution; malformed, duplicate-registered-key, depth-33, and
  over-64-KiB skipped vectors reject with exact content-free codes and no
  durable contribution. Depth 32 remains accepted.
- retirement enforcement: production Python sources and package metadata scan
  negative for thesis imports or thesis packaging; synthetic source-import and
  package mutations each fail with a code-owned rejection

## Fixture oracle answers

| Question | Content-free answer |
|---|---|
| tool | synthetic-claude |
| source time | 2026-01-02T03:04:05Z |
| model | claude-synthetic-v1 |
| project | synthetic-project |
| categories | input_tokens, output_tokens, cache_read_tokens, cache_write_tokens |
| amounts | 11, 7, 3, 2 |
| logical request | 1 |
| replay counts | unchanged |
| health | healthy |

The focused requirement/matrix suite records one event, four registered amounts, one
logical request, one sequence, four aggregates, one synthetic obligation, and
one cursor. The six named public reads derive the table above from an
independent fixed-fixture oracle. A line/file/restart/rescan A/B/A vector
preserves the original contribution and durable `identity_collision` state.
The parser's recorded framing evidence proves the JSONL record is a shared
`memoryview` over a read-only mapped source buffer, not a whole-line copy;
skipped sentinel bytes are neither decoded nor materialized into an application
value.
