# Synthetic thesis run record

## Result

- `elapsed_time`: `600` seconds inclusive
- stable-view/health reads after setup: `6`
- private-table reads: `0`
- checkpoint: `PASS`
- mutation vectors: deliberate leak, decoder, materializer, fingerprint, and
  network mutations each failed the harness
- capture canaries: application-value, parser-instrumentation, log, exception,
  crash, SQLite, sink, image, environment, and network lanes each observed a
  distinct harmless positive control

## Fixture oracle answers

| Question | Content-free answer |
|---|---|
| tool | synthetic-claude |
| source time | 2026-01-02T03:04:05Z |
| model | claude-synthetic-v1 |
| project | synthetic-project |
| categories | cache_read_tokens, cache_write_tokens, input_tokens, output_tokens |
| amounts | 11, 7, 3, 2 |
| logical request | 1 |
| replay counts | unchanged |
| health | healthy |

The exact six-test command records one event, four registered amounts, one
logical request, one sequence, four aggregates, one synthetic obligation, and
one cursor. A/B/A identity mutation preserves the original contribution and
reports `identity_collision`; no raw fixture content is copied into this
record.
