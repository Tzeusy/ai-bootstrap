# Launch-gate administration — 2026-08-10, commit `96ba99dda1503e2f278df9b76abd3c5872faa8fd`

Reviewer: OpenAI Codex, GPT-5 family; exact deployment version not exposed
Reviewer model family: GPT-5; cross-administration model alternation cannot be determined from the permitted materials
Fresh context: yes — no prior gate results, reviews, authoring history, repository history, memory, transcripts, or administrator key were supplied or read
Repository: `/home/tze/.dotfiles/ai-bootstrap`
Administration order: A → B → C → D → E → F → G; D completed before E3
Evidence convention: every citation below names lines from the exact commit above

## Materials given

1. `skills/personal/th-projects/subskills/project-direction/references/launch-gate.md`, v1.1.
2. `projects/ai-usage-telemetry/about/heart-and-soul/vision.md`.
3. `projects/ai-usage-telemetry/about/README.md`.
4. `projects/ai-usage-telemetry/docs/launch-gate/parameters.md`, v1.4.
5. Fixed 17-file `SHAPE_CORPUS`:

   1. `projects/ai-usage-telemetry/about/legends-and-lore/README.md`
   2. `projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md`
   3. `projects/ai-usage-telemetry/about/legends-and-lore/evidence/0001-provenance.md`
   4. `projects/ai-usage-telemetry/about/legends-and-lore/evidence/0001-source-and-bounds.md`
   5. `projects/ai-usage-telemetry/about/legends-and-lore/evidence/0001-synthetic-vectors.md`
   6. `projects/ai-usage-telemetry/about/lay-and-land/README.md`
   7. `projects/ai-usage-telemetry/about/lay-and-land/components.md`
   8. `projects/ai-usage-telemetry/about/lay-and-land/data-flow.md`
   9. `projects/ai-usage-telemetry/about/lay-and-land/deployment.md`
   10. `projects/ai-usage-telemetry/about/lay-and-land/assets/README.md`
   11. `projects/ai-usage-telemetry/about/craft-and-care/README.md`
   12. `projects/ai-usage-telemetry/about/craft-and-care/engineering-bar.md`
   13. `projects/ai-usage-telemetry/about/craft-and-care/testing-and-verification.md`
   14. `projects/ai-usage-telemetry/about/craft-and-care/security-and-privacy.md`
   15. `projects/ai-usage-telemetry/about/craft-and-care/review-and-documentation.md`
   16. `projects/ai-usage-telemetry/about/craft-and-care/observability-and-operations.md`
   17. `projects/ai-usage-telemetry/about/craft-and-care/interfaces-and-dependencies.md`

6. At F1 only, `projects/ai-usage-telemetry/docs/launch-gate/trend.md` lines 3–17: its table and comparability notes only.

Required skill-routing instructions were consulted for procedure only and were not used as substantive gate evidence.

## Material deviations and protocol conflict

No substantive evidence was added outside the allowlist. No allowlisted evidence file was unreadable.

One administration material remains unavailable:

- The E4 classifications and nonce are intentionally outside the repository. The parameter block publishes only their SHA-256 commitment and says the key is revealed after comparison (`parameters.md:125-130`).
- Launch-gate v1.1 provides no exact-commit mechanism within the allowlist for obtaining or comparing that key.
- The strict administration instruction prohibits inspecting or inferring any external key.
- I therefore fixed all five independent E4 classifications, performed no key access or comparison, and preserved E4 as `Unknown(reason)`. No nonce or project answer key is disclosed.

## Tier drops

None. C3 and C4 were administered.

## Operationalization notes

- A5 “major shape commitments” means the RFC’s enumerated D1–D10 decisions; the named product consumer is the developer identified in the goal statement.
- A6 treats an expressly uncommitted deadline, staffing level, and hosted-service budget as a bounded zero-assumption envelope, not an implied resource.
- B1 value includes independently reviewable and durable acceptance decisions; only the Synthetic-to-SQLite spine is authorized to yield an early executable thesis test.
- C2 distinguishes a governing owner from derived restatement: doctrine owns constitutional boundaries; RFC 0001 owns design mechanics; OpenSpec will own observable acceptance scenarios; topology owns placement; craft-and-care owns change evidence.
- D1 was performed as an actual fresh-engineer exercise, not inferred from document titles.
- D2 used exactly the three pre-bound tasks.
- E3 treats exact profile values, schema columns, and WHEN/THEN cases as downstream specification choices where the RFC has already fixed their shape-level constraints.
- E4 uses three classifications: `Shape`, `Spec`, and `Borderline — split required`, following RFC 0001’s rule that compound shape/spec sentences must be decomposed.
- F1 compares the prior aggregate trend row with this administration’s provisional `0 Not met / 1 Unknown`, while excluding the replaced E4 candidates from any claim of artificial convergence.
- F2’s reachability denominator is all 17 fixed shape-corpus files. The current support ratio is 11 normative/design/standard artifacts to 6 routing/evidence/meta artifacts.
- No owner approval, waiver, adoption, acceptance, or deferral is inferred.

## Verdict summary

| Q | Verdict |
|---|---|
| A1–A6 | Met |
| B1–B5 | Met |
| C1–C6 | Met |
| D1–D4 | Met |
| E1 | Met; all five sub-verdicts Met |
| E2 | Met |
| E3 | Met; reopen-list empty after trace |
| E4 | Unknown(reason: precommitted answer key and nonce unavailable under the allowlist; no comparison mechanism in v1.1) |
| E5–E6 | Met |
| F1–F4 | Met |
| G1 | Completed; no closed verdict |
| P1–P6 | Met |

No aggregate score is assigned.

# A. Problem and vision

## A1

> **A1 [U]** Does the project name a specific, *lived* human problem — one whose resolution would be observable in a specific person's behavior?
> *Fails when:* the problem is a category ("agent observability") rather than an experience.

Verdict: `Met`

Evidence:

- The project serves “any developer who wants one durable account of tokens, unique usage requests, and quota state” without surrendering content or credentials (`projects/ai-usage-telemetry/about/heart-and-soul/vision.md:5-8`).
- Success is observable: the developer leaves one container running and queries durable history and quota state (`vision.md:143-150`).

Falsification attempt: I tried to reduce the statement to the category “AI observability.” It survived because it names a person, missing capability, privacy cost, and concrete query behavior.

## A2

> **A2 [U]** Is the thesis falsifiable — does the project state what evidence would prove it wrong, and when that judgment gets made?
> *Fails when:* every conceivable outcome can be narrated as success, or the falsifier has no trigger point.

Verdict: `Met`

Evidence:

- The checkpoint occurs after the Synthetic-to-SQLite capability and disposable harness are accepted, but before sibling implementation (`projects/ai-usage-telemetry/docs/launch-gate/parameters.md:21`).
- Wrong answers, source/schema archaeology, content or credential access, remote-service dependence, or duplicate replay contributions explicitly falsify the bounded legibility thesis; architecture or projection-repair failures falsify the portable-service thesis before release (`parameters.md:21`).

Falsification attempt: I looked for a judgment with neither time nor failure threshold. Both thesis checkpoints have explicit lifecycle positions and failure conditions.

## A3

> **A3 [U]** Are the exclusion boundaries sharp enough to *reject a plausible near-miss* — could a reviewer, citing only the goal statement, refuse a product that superficially resembles the goal?
> *Fails when:* the is/is-not list only excludes strawmen nobody would build.
> *Administer concretely:* judge the pre-written `NEAR_MISSES` from the parameter block, accepting or rejecting each with a citation — never invent softer near-misses on the spot.

Verdict: `Met`

Near-miss decisions, using only the goal statement:

1. **Local AI Workbench Journal — Rejected.** Its full-text session import conflicts with “not a prompt, response, conversation, or tool-call auditing system” (`vision.md:25-27`) and the prohibition on decoding or materializing content (`vision.md:55-66`).
2. **AI Plan Optimizer — Rejected.** Its price/invoice optimization conflicts with “not a billing ledger, token-cost calculator, or promise of provider invoice accuracy” (`vision.md:27-29`).
3. **Quota Guardian — Rejected.** Its authenticated vendor API fallback conflicts with “not a cloud-account collector” and “must not require provider or tool credentials” (`vision.md:30-32`), while unsupported quota coverage remains explicitly unpromised (`vision.md:35-36`).

Falsification attempt: Each near-miss preserves attractive portions of the goal—local ownership, usage history, or content exclusion. The constitutional exclusions still reject each without relying on shape-corpus implementation details.

## A4

> **A4 [U]** Is the problem tractable as scoped: does a smallest end-to-end slice exist that exercises the core loop and could be built and honestly evaluated with bounded effort?
> *Fails when:* the first demonstrable value requires most of the system.

Verdict: `Met`

Evidence:

- The first candidate is one synthetic Claude record projected without content, normalized, persisted, replayed idempotently, and exposed through one local view (`parameters.md:53-59`).
- It explicitly excludes Codex, real mounts, production packaging, remote sinks, complete health, and other v1 concerns (`parameters.md:60-74`).

Falsification attempt: I tested whether the slice secretly required both adapters, production deployment, or remote sinks. The charter excludes them and authorizes only a disposable synthetic thesis-test harness.

## A5

> **A5 [U]** Are the project's consumers enumerated, and does every major shape commitment trace to a named consumer's need — and every named consumer to at least one commitment that serves it?
> *Fails when:* a consumer exists only in rhetoric, or a shape element serves no one who was named.

Verdict: `Met`

Evidence:

- The consumer is the developer seeking durable, content-free local usage history (`vision.md:5-8`).
- All ten major design decisions D1–D10 trace to named doctrine principles (`projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md:95-111`).
- Those principles culminate in the developer-observable success behavior (`vision.md:143-150`).

Falsification attempt: I swept the ten enumerated decisions for an untraced commitment and found none. I also looked for a separately named consumer with no serving commitment; no second product-consumer class is asserted.

## A6

> **A6 [U]** Is the scope achievable with the resources the project actually has — people, attention, money, calendar — stated rather than implied?
> *Fails when:* the shape silently assumes a team, budget, or cadence the project does not have.

Verdict: `Met`

Evidence:

- One human decision-maker and repository-agent assistance are observed.
- Additional staffing, weekly hours, deadline, and paid hosted-service budget are explicitly unknown; v1 must assume none, keep capabilities bounded, and stop for a new owner decision before depending on any (`parameters.md:22`).

Falsification attempt: I looked for hidden team, deadline, service, or parallel-review assumptions. The resource envelope explicitly forbids relying on all of them.

# B. Decomposability and sequencing

## B1

> **B1 [U]** Does the shape decompose into chunks each independently understandable, independently acceptable-or-rejectable, and valuable before later chunks exist?
> *Fails when:* acceptance is practically all-or-nothing, whatever the packaging says.

Verdict: `Met`

Evidence:

- Each initial capability is independently reviewable, acceptable, or rejectable; rejection blocks the full implementation boundary without invalidating accepted siblings (`parameters.md:66-71`).
- The independently accepted spine can produce a disposable thesis-test harness before sibling capabilities (`parameters.md:72-74`; RFC 0001:1173-1179).

Falsification attempt: I tested whether sibling rejection erased the spine or forced all-or-nothing acceptance. The corpus expressly preserves sibling decisions and their evidence.

## B2

> **B2 [U]** Is chunk ordering explicit and acyclic — does no chunk's acceptance silently presume an unaccepted one?
> *Fails when:* a chunk's text relies on a sibling that could still be rejected, with no stated fallback.

Verdict: `Met`

Evidence:

- The spine is the first candidate; all other RFC-required outcomes are companion capabilities in the same initial changeset (`parameters.md:53-71`).
- Only the disposable spine harness may precede sibling acceptance; non-synthetic implementation and release wait for all required capabilities (`parameters.md:72-74`; RFC 0001:1164-1179).

Falsification attempt: I looked for a cycle where accepting the spine depended on an accepted sink, runtime, or recovery sibling. Those siblings block broader implementation, not the spine’s independent acceptance.

## B3

> **B3 [U]** Does the first buildable slice generate *evidence about the thesis itself*, not just infrastructure?
> *Fails when:* the slice proves the team can build plumbing but nothing about whether the product idea is right.

Verdict: `Met`

Evidence:

- The slice must let an owner-designated developer answer prewritten source, time, token-category, and contribution questions within 15 minutes, without archaeology or forbidden access (`parameters.md:21`).
- The candidate’s acceptance boundary is record → durable ledger → local view (`parameters.md:53-61`).

Falsification attempt: I treated SQLite persistence alone as mere plumbing. The required human query exercise turns the slice into direct evidence about legibility and queryability.

## B4

> **B4 [U]** Can any single chunk be rejected without collapsing the whole — is partial acceptance a designed state rather than an accident?
> *Fails when:* rejecting one chunk invalidates digests, references, or premises across the others.

Verdict: `Met`

Evidence:

- A rejected companion blocks implementation and release but “does not invalidate an accepted sibling capability or the thesis evidence it produces” (`parameters.md:66-71`).
- RFC 0001 preserves each capability’s decision and trace independently (`RFC 0001:1164-1171`).

Falsification attempt: I rejected an arbitrary companion in the documented model. The overall release remained blocked, but accepted sibling decisions remained valid.

## B5

> **B5 [U]** Does the sequencing retire risk early — are the assumptions most likely to invalidate the thesis scheduled to be tested in the earliest chunks that can test them?
> *Fails when:* early chunks are the comfortable ones and every thesis-threatening unknown lives late — or nobody can name the riskiest assumption at all.

Verdict: `Met`

Evidence:

- The first slice directly tests content-free legibility, queryability, and duplicate-replay accounting before sibling implementation (`parameters.md:21,53-74`).
- Still-unproved adapter profiles remain disabled until pinned evidence and vectors exist (`RFC 0001:86-93`).
- Unmeasured numeric profiles block real mounts, durable ingestion, remote export, and release (`projects/ai-usage-telemetry/about/legends-and-lore/evidence/0001-source-and-bounds.md:157-165`).

Falsification attempt: I looked for a comfortable framework-first sequence. The first executable work instead attacks the principal lived thesis, while source and release uncertainties are activation gates rather than deferred assumptions.

# C. Authority discipline

## C1

> **C1 [U]** Does authority flow one way — doctrine → shape → specs → implementation — with each layer citing upward and no lower layer quietly redefining a higher one?
> *Fails when:* a lower artifact restates a higher rule with drifted meaning, or a summary is treated as the rule.

Verdict: `Met`

Evidence:

- The entry document says doctrine is owner-adopted, RFC 0001 accepted, topology and craft derived, and future implementation must trace through a signed-off requirement (`projects/ai-usage-telemetry/about/README.md:79-88`).
- RFC 0001 explicitly assigns topology, OpenSpec, and craft-and-care their downstream roles while retaining authority for design mechanics (`RFC 0001:1105-1117`).
- Shape owns load-bearing reasons; OpenSpec owns observable scenarios and exact accepted values (`RFC 0001:1197-1202`).

Falsification attempt: I compared entry, topology, craft standards, and RFC ownership claims for a reversed or competing authority edge. The routes consistently point upward.

## C2

> **C2 [U]** Is every normative "should" owned by exactly one artifact, with conflicts between artifacts resolved by rule rather than by reading order?
> *Fails when:* two artifacts answer the same question and a reader must guess which wins; or a rule lives only inside a validator, index, or generated report.
> *Scope note:* C2 asks whether exactly one owner **exists** (ownership); D2 asks whether a reader can **find** it (routing). Evidence for one is not evidence for the other.

Verdict: `Met`

Evidence:

- RFC 0001 owns adapter semantics, ledger atomicity, isolation, privacy/cardinality, and sink behavior; topology owns placement; OpenSpec owns testable requirements; craft-and-care owns the engineering bar (`RFC 0001:1105-1117`).
- If topology and contract disagree, the conflict is documentation drift and the governing decision must be resolved before implementation (`projects/ai-usage-telemetry/about/lay-and-land/README.md:15-20`).
- Compound shape/spec sentences must be decomposed into their separate owners (`RFC 0001:1197-1202`).

Falsification attempt: I treated repeated operational language in craft-and-care as a second design authority. Its router says those exact mechanics belong to RFC 0001 and the standards govern how changes are engineered (`projects/ai-usage-telemetry/about/craft-and-care/interfaces-and-dependencies.md:8-11`).

## C3

> **C3 [P]** Does the project apply its own epistemics *to itself* — are claims about its own state evidence-backed and labeled ([Observed] / [Inferred] / [Unknown]), with absence of evidence rendered as unknown, never as done?
> *Fails when:* a status document claims green/zero/complete without a sweep run against the current bytes.

Verdict: `Met`

Evidence:

- The entry defines `[Observed]`, `[Inferred]`, and `[Unknown]`, distinguishes the adopted baseline from implementation evidence, and says state-bearing changes become Unknown until reswept (`about/README.md:7-21`).
- Its lifecycle matrix labels doctrine, RFC, topology, standards, and absent OpenSpec individually (`about/README.md:23-29`).
- The source annex explicitly defines the same evidence meanings and fail-closed unknown treatment (`evidence/0001-source-and-bounds.md:19-21`).

Falsification attempt: I looked for a current implementation, release, or complete-profile claim unsupported by evidence. The corpus instead repeatedly identifies pre-implementation status and unresolved profile evidence.

## C4

> **C4 [P]** Are the human decision points enumerated and non-delegable — can a reader determine *from artifacts alone* whether any given decision was actually made by a human?
> *Fails when:* an agent-authored artifact could pass as an owner act, or a pending decision is indistinguishable from a made one.

Verdict: `Met`

Evidence:

- Tze is the named human decider; agents cannot adopt doctrine, accept an RFC/spec, waive a gate, or expand irreversible scope (`parameters.md:19`).
- The status matrix distinguishes adopted/accepted artifacts from absent OpenSpec and names the next gate (`about/README.md:23-40`).
- Each capability’s acceptance authority is expressly Tze, with conditional boundaries enumerated (`parameters.md:139`).

Falsification attempt: I tried to interpret the current reviewer packet or an accepted-looking draft as an owner act. The explicit delegation rule prevents that.

## C5

> **C5 [U]** Does the shape separate what the project controls from what it assumes of its substrate — external tools, formats, platforms — with each load-bearing assumption recorded alongside a stated posture if it breaks?
> *Fails when:* an external dependency is treated as guaranteed, or its failure would reshape the project and no artifact admits that.

Verdict: `Met`

Evidence:

- The RFC distinguishes observed Claude/Codex evidence, unavailable Claude quota, and deferred OpenCode support (`RFC 0001:63-93`).
- Each source begins `unsupported_profile`; unrecognized builds or unproved identity/arithmetic hold rather than guess (`RFC 0001:86-93,446-454`).
- Future tools are explicitly unknown and not assumed to fit the adapter contract (`projects/ai-usage-telemetry/about/lay-and-land/components.md:71-84`).

Falsification attempt: I tested format drift, absent quota, unknown tools, and unmeasured limits. Each has a fail-closed or deferred posture.

## C6

> **C6 [U]** Does required authority scale with irreversibility — are hard-to-undo or externally visible actions gated more strongly than routine, revertable ones?
> *Fails when:* one approval level covers everything, or an irreversible effect is reachable through a routine path.

Verdict: `Met`

Evidence:

- Routine configuration can select sources, aliases, cadence, and projections but cannot change identity or release-profile rules (`RFC 0001:173-211`).
- Doctrine, RFC/spec acceptance, waivers, and irreversible scope remain human decisions (`parameters.md:19`).
- Privacy repair requires an owner-approved isolated plan and blast-radius review (`RFC 0001:670-677`).

Falsification attempt: I tried to reach credential access, history deletion, or privacy repair through configuration. Each lies outside routine authority.

# D. Comprehensibility

## D1

> **D1 [U]** Fresh-engineer test: can a capable engineer with no prior contact state the problem, thesis, current lifecycle stage, and next pending decision after bounded reading (≤ 60 min)?
> *Fails when:* correctness requires archaeology — reading history, resolving contradictory metadata, or knowing which documents are stale.

Verdict: `Met`

Fresh-engineer outcome reached within the bounded administration:

- Problem: a developer lacks one durable, queryable, content-free account of supported local AI-tool usage and quota facts.
- Thesis: one local service can normalize supported records into an authoritative SQLite ledger and independently project optional sinks.
- Stage: doctrine, RFC 0001, topology, and craft standards are adopted/accepted; OpenSpec and implementation are absent.
- Next pending decision: this named-commit launch gate determines whether the objection to authoring the first OpenSpec changeset is removed; any actual start or acceptance remains an owner act.

Evidence: `vision.md:5-21,143-154`; `about/README.md:7-29,41-46,57-61`.

Falsification attempt: I did not consult history or prior records. The front door and authority/status sections supplied all four answers without contradictory metadata.

## D2

> **D2 [U]** Task-routing test: from the front door, can they reach the *single* rule governing one concrete task without exhaustive reading?
> *Administer concretely:* use the tasks fixed in the parameter block (`D2_ROUTINE_TASK`, `D2_AUTHORITY_TASK`, `D2_SEAM_TASK`), chosen before the administration — never by the reviewer or the administering session mid-run. At least one task must cross a seam between two chunks.
> *Fails when:* the route requires reading the corpus, or two routes give two answers.

Verdict: `Met`

The three fixed routes:

1. `D2_ROUTINE_TASK`: configure a local account alias.

   Route: `about/README.md:73-74` → RFC 0001’s Configuration Contract, `RFC 0001:173-197`.

   Governing rule: aliases are configuration/display values; immutable technical namespaces remain separate, and credentials cannot enter TOML.

2. `D2_AUTHORITY_TASK`: permit credential-store access for identity.

   Route: `about/README.md:65-70` → `vision.md:55-66`.

   Governing rule: credential stores must never be mounted; no configuration weakens the rule. RFC 0001 confirms that this requires an owner-adopted doctrine amendment (`RFC 0001:1204-1224`).

3. `D2_SEAM_TASK`: add a field through extraction, ledger admission, and PostgreSQL.

   Route: `about/README.md:73-74` → RFC 0001’s Privacy and Cardinality Budget, `RFC 0001:1041-1057`.

   Governing rule: extraction, ledger, PostgreSQL, and OTLP controls are distinct; permission at one grants none at another.

Falsification attempt: I attempted an alternate route through topology or craft standards. Both point back to RFC 0001 rather than producing competing answers.

## D3

> **D3 [U]** Is invented vocabulary minimal and defined-before-use — does each coined term buy clarity ordinary language couldn't?
> *Fails when:* the default reading path uses a term before defining it, or a coined term shadows an ordinary word's meaning.

Verdict: `Met`

Evidence:

- The primary coined abstraction, “release profile,” is formally defined before the design sections use it (`RFC 0001:53-61`).
- `UsageEvent` and `QuotaSnapshot` receive explicit field contracts before downstream use (`RFC 0001:308-350,384-442`).
- Source stream, cursor tuple, ledger sequence, and checkpoint semantics are defined at their owning sections (`RFC 0001:583-594,684-708,851-860`).

Falsification attempt: I traced the default reading route for unexplained coined terms or ordinary-word shadowing. Abstractions either receive an owning definition or retain their ordinary technical meaning.

## D4

> **D4 [U]** Do the entry/summary documents make no claim their sources don't — is simplification confined to presentation, never meaning?
> *Fails when:* an overview asserts something stronger, softer, or fresher than its owning source.

Verdict: `Met`

Evidence:

- The entry’s portable, durable, content-free summary (`about/README.md:3-12`) matches the vision (`vision.md:5-21`) and RFC ledger/privacy/runtime contracts (`RFC 0001:7-23`).
- The entry explicitly labels pre-implementation and absent-spec status (`about/README.md:7-12,23-29`); component and deployment summaries likewise identify inferred target state and absent implementation (`components.md:1-5`; `deployment.md:1-5`).

Falsification attempt: I compared summary claims about implementation, source support, quota availability, authority, and sinks against their governing sources. I found no stronger or fresher summary claim.

# E. Readiness to author specifications

## E1

> **E1 [U]** Is it defined what a specification *is* here — form, home, granularity, acceptance authority, and change process — before the first one is written?
> *Fails when:* the first spec author would have to invent the medium while writing the message.
> *Record sub-verdicts:* form, home, granularity, acceptance authority, change process — five answers, not one. E1 is Met only when all five are.

Overall verdict: `Met`

| Sub-verdict | Verdict | Evidence and falsification |
|---|---|---|
| Form | `Met` | Stable-ID normative requirements with Source, Scope, and adjacent WHEN/THEN scenarios are prescribed (`parameters.md:134-136`). I tried to find a missing normative/scenario form; none is left to the author. |
| Home | `Met` | Accepted specifications belong in `openspec/specs/`; active deltas and rationale in `openspec/changes/` (`parameters.md:137`). I looked for a competing spec home and found none. |
| Granularity | `Met` | One falsifiable vertical outcome with one primary acceptance boundary; other outcomes remain separate capabilities in the same initial changeset (`parameters.md:138`). I tested whether the first author must invent capability boundaries; the spine and companion rule already set them. |
| Acceptance authority | `Met` | Tze accepts or rejects each capability independently; the harness, full changeset, implementation, and release boundaries are separately gated (`parameters.md:139`). I looked for agent or implicit acceptance; neither is permitted. |
| Change process | `Met` | Amend through project-feature-request/spec delta, propagate accepted shape changes in the same change, and use reconciliation as backstop (`parameters.md:140`). I looked for an unspecified post-acceptance edit path; it is explicit. |

## E2

> **E2 [U]** Is the first specification identified, with every prerequisite either satisfied or explicitly waived on the record?
> *Fails when:* "what comes first" is answered differently by different documents, or prerequisites are discovered rather than listed.

Verdict: `Met`

Evidence:

- The first candidate and acceptance boundary are explicit (`parameters.md:51-74`).
- Six prerequisites are listed with satisfied or explicitly waived state (`parameters.md:76-85`).
- OpenSpec-directory absence is deliberately waived until a READY verdict (`parameters.md:83-85`).

Falsification attempt: I searched the entry and RFC for an alternate first specification or hidden prerequisite. They consistently describe one initial changeset led by the Synthetic-to-SQLite spine.

## E3

> **E3 [U]** Would authoring the first spec force reopening any doctrine- or shape-level question? Enumerate them.
> *The sharpest single gate:* hand the reviewer the shape corpus and the first spec's charter, and ask what they would have to reopen. An empty list — genuinely arrived at, not asserted — is the readiness signal.
> *Fails when:* the list is non-empty; "ready" is then false regardless of every other verdict.
> *Credibility protocol:* the reviewer first enumerates the first spec's central concepts and obligations, then traces each to the shape artifact that would govern it. An empty reopen-list **without this trace table** is recorded as Unknown, not Met — a shallow reading also produces an empty list.

Verdict: `Met`

Central-concept trace:

| Central concept or obligation | Governing shape evidence | Falsification result |
|---|---|---|
| One qualified synthetic Claude Code 2.1.226 record | Claude discriminator, permitted paths, native identity, accounting map, source time, and fingerprint are fixed in `RFC 0001:456-500`; version-specific evidence is in `evidence/0001-source-and-bounds.md:35-72`. | Exact values may become spec scenarios; no source-identity principle must be reopened. |
| Field projection without materializing content | Streaming projection and skip-only handling are fixed in `RFC 0001:217-277`; privacy tests in `security-and-privacy.md:26-38,92-101`. | Parser limits remain downstream profile values, but the boundary and failure posture are already settled. |
| Normalized content-free `UsageEvent` | Identity, fingerprint, attribution, amounts, and metadata contract are fixed in `RFC 0001:308-350`; categories in `RFC 0001:352-382`. | The spec need not decide whether content or arbitrary metadata is admissible. |
| One durable SQLite contribution | SQLite is the authority and atomically owns fact, amounts, aggregates, cursor, sequence, and sink obligations (`RFC 0001:574-622`). | Table details and scenarios can be specified without revisiting ledger authority or atomicity. |
| Replay produces no second fact/request contribution | Same-identity/same-fingerprint semantics are fixed in `RFC 0001:342-345,607-612`; the synthetic replay vector is explicit (`evidence/0001-synthetic-vectors.md:27-40`). | No deduplication-policy reopening is required. |
| Read-only local usage view | Stable local views and their compatibility rule are fixed in `RFC 0001:825-845`; `usage_events` and related view obligations are reiterated in `engineering-bar.md:49-51`. | Exact columns remain a spec artifact; local inspection versus inbound API is settled. |
| Disposable synthetic-only harness | Its permission and prohibitions are explicit in `parameters.md:72-74` and `RFC 0001:1173-1179`. | It cannot quietly become production architecture. |
| Human legibility/queryability thesis test | Timing, questions, failure conditions, and retained transcript are bound in `parameters.md:21`. | The spec can encode the checkpoint without reopening what success means. |
| Separation from sibling capabilities | Excluded outcomes and the same-initial-changeset rule are explicit in `parameters.md:60-71`; RFC capability allocation is fixed in `RFC 0001:1141-1171`. | Exact sibling requirements remain spec work, not unresolved shape work. |

E3 reopen-list: **empty**.

Specific attempted reopenings that did not survive:

- Exact parser ceilings: shape fixes the fail-closed profile mechanism; OpenSpec fixes/test-executes values.
- SQLite columns and nullability: shape fixes authority and invariants; OpenSpec freezes the observable schema.
- Crash behavior: atomicity and replay posture are already shape law.
- Whether synthetic work may precede siblings: explicitly authorized and bounded.
- Whether real mounts, sinks, production packaging, or release are implied: explicitly excluded.
- Whether content or credential access is negotiable: constitutional prohibition.

## E4

> **E4 [U]** Is the shape/spec boundary crisp enough that an author knows which side any given sentence falls on — without asking?
> *Administer concretely:* the reviewer classifies the five pre-written `E4_CANDIDATES` from the parameter block; disagreement with the project's own routing is a fail. Candidates are written before the administration, like `NEAR_MISSES` — a reviewer who picks their own examples picks easy ones.

Verdict: `Unknown(reason: independent classifications are fixed, but the precommitted project answer key and nonce are unavailable under the strict allowlist; v1.1 exposes no permitted exact-commit comparison mechanism)`

Independent classifications fixed before any comparison:

| Candidate | Fixed reviewer classification | Evidence and falsification |
|---|---|---|
| 1. “An adapter remains unsupported when its native identity is only guessed from collection order, even if sample records parse successfully.” | `Shape` | Identity cannot use collection order and unsupported identity holds the source (`RFC 0001:314-345,446-454`). I tried to convert it to a single observable test; its essential content is the governing authority/identity boundary. |
| 2. “Enabling PostgreSQL cannot make it authoritative for retained history or projection repair.” | `Shape` | SQLite is the accounting authority and sinks are replaceable projections (`vision.md:44-49`; `RFC 0001:574-581,851-873`). I tried to classify it as a PostgreSQL acceptance scenario, but the sentence defines system authority. |
| 3. “When a Codex rate-limit-only record repeats unchanged usage counters, it updates eligible quota state without adding a token or request contribution.” | `Spec` | The shape fixes the semantic rule (`RFC 0001:532-563`), while this sentence is already a concrete input-condition/output-behavior scenario suitable for WHEN/THEN acceptance. I tested for a remaining architectural choice; none is expressed. |
| 4. “When a supported source has no registered local quota observation, its read-only quota view reports unavailable rather than zero.” | `Spec` | Shape fixes unavailable versus zero (`RFC 0001:423-437`); the sentence specifies observable view behavior. I tried to treat “unavailable” as doctrine only, but the exact view outcome makes it specification-form behavior. |
| 5. “A quota value is current only while its source-observation time remains within the active profile's freshness deadline.” | `Borderline — split required` | Shape owns source-observation time and the fact that freshness cannot use collection time; the profile/spec owns the exact deadline and boundary scenario (`RFC 0001:415-432,1197-1202`). Assigning the whole sentence to either layer would collapse that division. |

No comparison was performed. The key-commitment statement is at `parameters.md:125-130`. Evidence that would settle E4 is the precommitted answer key and nonce supplied through an authorized channel, followed by SHA-256 verification and direct comparison against the classifications above.

## E5

> **E5 [U]** Do acceptance criteria exist for a spec itself — how one will be judged complete, testable, and faithful to the shape above it?
> *Fails when:* spec acceptance would be a vibe check by whoever reviews it.

Verdict: `Met`

Evidence:

- The prescribed form includes stable identity, Source, Scope, and adjacent WHEN/THEN scenarios (`parameters.md:134-136`).
- Capability granularity and explicit human acceptance are bound (`parameters.md:138-139`).
- RFC 0001 enumerates what the initial changeset must freeze and test and requires separately reviewable capability deltas (`RFC 0001:1141-1171`).

Falsification attempt: I looked for a path where attractive prose could pass without traceability, falsifiable scenarios, mechanical checks, independent cycles, or owner acceptance. The binding requires all of them.

## E6

> **E6 [U]** Is there a defined propagation path for a shape change *after* specs exist — how affected specs are detected, who amends them, and how the interim disagreement is surfaced rather than hidden?
> *Fails when:* the first post-spec shape amendment would create silent contradictions between layers.
> *Default binding:* th-projects answers this with per-change spec sync (feature-request amendment mode) plus episodic reconciliation as backstop; Met still requires the project to have adopted it, not merely inherited it.

Verdict: `Met`

Evidence:

- The project explicitly adopts feature-request/spec-delta amendment, same-change propagation, and project-review reconciliation (`parameters.md:140`).
- Accepted load-bearing changes require an RFC amendment or successor plus downstream spec updates (`RFC 0001:1080-1095`).
- Same-change documentation and downstream reconciliation are mandatory (`projects/ai-usage-telemetry/about/craft-and-care/review-and-documentation.md:62-72`).

Falsification attempt: I looked for an interval in which implementation could continue under contradictory shape and spec. Same-change propagation and explicit review block that path.

# F. Process health and convergence

## F1

> **F1 [U]** Is the improvement cycle *converging* — are successive review rounds finding fewer and less severe defects, with a declared stop condition other than exhaustion?
> *Requires the trend log (§5).*
> *Fails when:* each round's fixes mint the next round's findings, or no one can state what would end the cycle.

Verdict: `Met`

Evidence:

- The permitted prior trend row records 8 Not-met, 1 Unknown, 8 new findings, and NOT READY (`projects/ai-usage-telemetry/docs/launch-gate/trend.md:3-5`).
- This full administration produces 0 Not-met and 1 Unknown, the latter caused by the unavailable E4 key rather than a demonstrated corpus defect.
- The comparability notes say no U/P/G question changed; replacement E4 candidates must not be narrated as convergence; instrument v1.1 did not change any question or verdict rule (`trend.md:7-17`).
- The stop condition is the unchanged §4 rule: every E Met, no A–D Not-met, and F1 not diverging.

Falsification attempt: I tested whether the apparent improvement came from weaker questions. The notes explicitly say the questions and verdict rule are unchanged, while E4 was made harder and is excluded from artificial convergence.

## F2

> **F2 [U]** Is the governance corpus proportionate to what it protects — does each artifact have an owner and a retirement path, and does repairing a defect not routinely create new artifacts?
> *Fails when:* the corpus grows monotonically; defect → new report → new validator → new defect.
> *Operational proxies:* artifact-count trend across cycles; the ratio of normative artifacts to meta-artifacts (reports, validators, indexes) — meta should not outgrow normative; a sweep for artifacts no reading route reaches.

Verdict: `Met`

Evidence:

- Every artifact class has an accountable owner and retirement or supersession path (`review-and-documentation.md:74-89`).
- The default route is constrained, and repairs should amend existing owners rather than create reports or validators merely to restate findings (`review-and-documentation.md:91-96`).
- Reachability sweep: 17/17 corpus files are routed—five through Legends-and-Lore (`about/README.md:54`; `legends-and-lore/README.md:14-22`), five through Lay-and-Land (`about/README.md:55`; `lay-and-land/README.md:8-13`), and seven through Craft-and-Care (`about/README.md:53`; `craft-and-care/README.md:15-31`).
- Current ratio: 11 normative/design/standard artifacts to 6 routing/evidence/meta artifacts. Meta does not outgrow normative.

Falsification attempt: I searched for an orphaned fixed-corpus file, an artifact class without retirement, and a meta-heavy ratio. None was found. Historical artifact-count data beyond the permitted trend table was unavailable and was not inferred.

## F3

> **F3 [U]** Can the owner make each acceptance decision from a bounded packet in one sitting, without archaeology?
> *Fails when:* the honest answer to "what am I binding?" requires reading history or trusting a summary the packet itself calls non-authoritative.

Verdict: `Met`

Evidence:

- Each capability is independently reviewable and independently accepted or rejected by Tze (`parameters.md:138-139`).
- Author evidence must provide doctrine/RFC/spec/topology trace, risk, assumptions, commands, fixtures, failure evidence, and changed contracts (`review-and-documentation.md:6-20`).
- Blocking-review dispositions and evidence are explicit rather than conversational (`review-and-documentation.md:22-40`).

Falsification attempt: I tried to make acceptance depend on prior conversation or repository archaeology. The required packet carries the binding, evidence, findings, and disposition at the current revision.

## F4

> **F4 [U]** If all improvement stopped today, would the corpus be safe to abandon — no artifact left misstating the current state to a future reader?
> *Fails when:* any stale claim, superseded offering, or dead route is reachable from a default reading path without a banner.

Verdict: `Met`

Evidence:

- The front door clearly says the project is pre-implementation, identifies proposed versus observed claims, and does not claim release evidence (`about/README.md:7-12`).
- The lifecycle matrix labels specs absent and implementation absent (`about/README.md:23-29`).
- Topology documents identify themselves as inferred target state with no application package/runtime image (`components.md:1-5`; `deployment.md:1-5`).
- Historical reviews and administrations are deliberately off the default route (`about/README.md:41-46`).

Falsification attempt: I swept the default route for a reachable claim that the service, profile evidence, OpenSpec, deployment, or release already exists. Each such boundary remains explicitly pre-implementation, absent, inferred, or gated.

# G. Gate self-scrutiny

## G1

> **G1 [U]** Completeness critic — the reviewer answers: *what readiness dimension could this project fail that no question above would catch?*
> Proposed missing questions go into the record and are considered for amendment of this instrument (§9).
> *G1 yields no Met/Not-met verdict; an administration that skips it is incomplete and cannot support a gate decision.*

Completed; no closed verdict.

Proposed missing question:

> **Evidence-expiry readiness:** Are volatile, version-specific substrate claims assigned an explicit revalidation trigger before a first specification treats them as current?
> *Fails when:* evidence is pinned but can age indefinitely without a release-profile, source-version, elapsed-time, or upstream-change event forcing revalidation.

Why it is not fully covered: C5 requires assumptions and failure posture, while F4 catches already-stale reachable claims. Neither directly requires a forward-looking expiry/revalidation trigger before specification authoring.

Current project posture appears strong—the evidence is version-pinned and unsupported profiles fail closed—but the readiness dimension could fail in another project without clearly failing an existing question.

# Goal-derived questions

## P1

> **P1 [G]** Does the first-spec candidate preserve a durable local account of accepted facts rather than making an optional sink the effective source of truth?
> *Fails when:* a sink outage, endpoint change, or projection policy can erase, replace, or redefine the user's history.
> *Doctrine basis:* `vision.md`, **1. Local Facts Become User-Owned History.**

Verdict: `Met`

Evidence: the candidate persists one durable SQLite contribution (`parameters.md:53-61`); SQLite is authoritative and optional sinks retain independent pending state (`RFC 0001:574-622,851-873`).

Falsification attempt: I disabled or failed both remote sinks in the documented model. The local ledger and view remain authoritative.

## P2

> **P2 [G]** Does the candidate prohibit content and credentials across source collection, diagnostics, persistence, and export?
> *Fails when:* any supported outcome requires decoding, retaining, or emitting them, including through a convenient error or enrichment path.
> *Doctrine basis:* `vision.md`, **2. Content and Credentials Stay Outside.**

Verdict: `Met`

Evidence: the candidate explicitly field-projects without materializing content and produces a content-free ledger (`parameters.md:53-61`); parser, diagnostics, ledger, and destination prohibitions are explicit (`RFC 0001:217-277,1041-1074`).

Falsification attempt: I tested parse errors, diagnostics, fingerprinting, and future export as leakage shortcuts. All are deny-by-default.

## P3

> **P3 [G]** Does the candidate make accounting eventually exact for every supported observable fact across rescans, crashes, and retries while refusing to claim unknowable pre-discovery coverage?
> *Fails when:* a supported fact can be silently lost or counted twice, or unknown coverage is narrated as reconciled history.
> *Doctrine basis:* `vision.md`, **3. Accounting Is Eventually Exact.**

Verdict: `Met`

Evidence: the candidate requires replay without a second contribution (`parameters.md:53-61`); stable identity, atomic commit, rollback, and replay rules are fixed (`RFC 0001:308-345,574-622`); pre-discovery coverage remains `coverage_unknown` (`RFC 0001:43-50,794-795`).

Falsification attempt: I replayed, restarted around a transaction, and removed pre-discovery evidence conceptually. The contract respectively deduplicates, rolls back/retries, and reports unknown rather than complete.

## P4

> **P4 [G]** Does the candidate make source, storage, and sink failure explicit and scoped without masking degradation or stopping unrelated healthy paths?
> *Fails when:* aggregate health appears successful while a stream is held/stale, or one failed boundary blocks unrelated collection.
> *Doctrine basis:* `vision.md`, **4. Partial Failure Is Explicit.**

Verdict: `Met`

Evidence: the candidate does not claim ownership of complete multi-stream, storage, or sink behavior, and those required outcomes remain companion capabilities in the same initial changeset (`parameters.md:60-71`). The governing shape requires source-scoped quarantine, storage holds, independent sinks, and non-masking health (`RFC 0001:784-845`).

Falsification attempt: I failed one stream, SQLite admission, and each sink independently. The shape exposes each failure at its proper scope and prevents aggregate healthy claims.

## P5

> **P5 [G]** Does the candidate preserve the narrow portable runtime boundary?
> *Fails when:* it requires broad/writable source mounts, an auth store, host scheduler, inbound API, elevated runtime, or architecture-specific semantics.
> *Doctrine basis:* `vision.md`, **6. The Runtime Boundary Is Portable and Narrow.**

Verdict: `Met`

Evidence: the early harness is disposable, synthetic-only, reads no personal mount, opens no network path, and ships no production package (`parameters.md:72-74`; `RFC 0001:1173-1179`). The eventual runtime boundary remains non-root, explicit-mount, no-inbound, dual-architecture (`RFC 0001:115-167,1119-1135`).

Falsification attempt: I attempted to require a real source mount, network service, root privilege, or single-architecture shortcut for the first slice. Each is prohibited.

## P6

> **P6 [G]** Does the candidate preserve evidence-backed source meaning through normalization rather than merely persisting a stable but semantically wrong record?
> *Fails when:* it merges unlike token categories, guesses an unavailable dimension, substitutes collection time for source observation time, or loses the source identity needed to interpret the contribution.
> *Doctrine basis:* `vision.md`, **5. Normalization Preserves Meaning.**

Verdict: `Met`

Evidence: Claude identity, source time, model, project handling, accounting map, and inclusive output semantics are fixed (`RFC 0001:456-500`); normalized categories are non-overlapping and unknown dimensions remain unknown (`RFC 0001:308-382`).

Falsification attempt: I tried overlapping output categories, guessed attribution, collection-time substitution, and offset-based identity. The governing contract rejects each.

# Unknowns and what would settle them

1. **E4:** The independently fixed classifications cannot be compared with the project’s precommitted answer key.

   Settlement requires:

   - authorized delivery of the precommitted answer key and nonce after these classifications were fixed;
   - verification that their bytes hash to `0fff71d85d4d599989a704313bedd9996e52ae696edabfaa523a2d1967366f6b`;
   - direct comparison with the five classifications recorded above;
   - disclosure of the verified nonce and key in the immutable gate record, as required by `parameters.md:125-130`.

No inference from the commitment hash is permitted.

# Reviewer's falsification notes

Every closed question above includes its individual falsification attempt. The highest-risk attempts were:

- rejecting all three plausible near-miss products using doctrine alone;
- trying to construct an all-or-nothing or cyclic capability sequence;
- looking for a lower artifact that could override doctrine or RFC authority;
- routing all three pre-bound D2 tasks without corpus-wide reading;
- tracing the first specification’s central obligations before declaring an empty E3 reopen-list;
- fixing E4 classifications before any unavailable key comparison;
- testing whether apparent trend improvement resulted from weakened questions;
- sweeping all 17 shape-corpus files for unreachable artifacts;
- checking the default route for false implementation, specification, profile, or release claims.

# Proposed gate verdict per §4

**NOT READY**

Reason:

> Ready requires every E question `Met`.

E4 is `Unknown(reason)`, not `Met`. Therefore the gate cannot return READY even though:

- every A–D question is Met;
- E1, E2, E3, E5, and E6 are Met;
- E3’s reopen-list is empty;
- F1 is not diverging;
- G1 was completed; and
- P1–P6 are Met.

There are no `Not met` findings and therefore no doctrine, shape, comprehensibility, or spec-machinery remediation candidate to route. The sole blocker is completion of the protocol-prescribed, precommitted E4 key comparison through an authorized mechanism.

`READY-WITH-DEFERRALS` is not proposed: it is an owner-only act, and no owner approval or deferral was supplied.

No specification, changeset, bead, implementation work, or owner decision is authorized by this packet.

---

# Administrator completion after fixed reviewer classifications

This section records the administrator's post-review comparison after the reviewer completed and fixed all five E4 classifications. It does not alter the reviewer wording or provisional verdict above.

- Parameter commitment SHA-256: `0fff71d85d4d599989a704313bedd9996e52ae696edabfaa523a2d1967366f6b`.
- `/tmp/ai-usage-telemetry-e4-key-v1.4-20260810.md` SHA-256 independently verified by the administrator: `0fff71d85d4d599989a704313bedd9996e52ae696edabfaa523a2d1967366f6b`.
- Digest result: **exact match**.
- Nonce: `b34ea93e1c1689267fd05419dc47ba462abfcbf22ebf584f240b39305076aae1`.

The revealed precommitted key, compared only after the reviewer classifications were fixed:

| E4 candidate | Precommitted project routing | Reviewer's independently fixed routing | Match |
|---|---|---|---|
| An adapter remains unsupported when its native identity is only guessed from collection order, even if sample records parse successfully. | Shape: identity and activation invariant | Shape | Yes |
| Enabling PostgreSQL cannot make it authoritative for retained history or projection repair. | Shape: ledger and sink authority invariant | Shape | Yes |
| When a Codex rate-limit-only record repeats unchanged usage counters, it updates eligible quota state without adding a token or request contribution. | Spec: observable adapter/accounting behavior | Spec | Yes |
| When a supported source has no registered local quota observation, its read-only quota view reports unavailable rather than zero. | Spec: observable unknown-state behavior | Spec | Yes |
| A quota value is current only while its source-observation time remains within the active profile's freshness deadline. | Borderline: shape owns source-time semantics; specs own the exact profile deadline and boundary scenarios | Borderline — split required | Yes |

- Comparison result: **5/5 match**.

E4 final verdict: `Met`.

Final administration counts:

| Not met | Unknown | Deferred | Reopened | New findings vs prior |
|---:|---:|---:|---:|---:|
| 0 | 0 | 0 | 0 | 0 |

G1's general instrument suggestion is not a current-project defect.

Final gate verdict per §4: **READY**.

This comparison is the intended post-review completion mechanism. It is not owner acceptance, waiver, adoption, deferral, or implementation authorization. A READY gate verdict removes the readiness objection to beginning specifications; it does not itself authorize specification acceptance or implementation.
