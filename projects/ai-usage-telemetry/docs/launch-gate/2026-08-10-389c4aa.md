# Launch-gate administration — 2026-08-10, commit `389c4aab6704f4ec2ca4daa16b5b341abe140e89`

Reviewer: Codex GPT-5; exact serving version unavailable; fresh context: yes  
Reviewer model family: OpenAI GPT-5  
Named commit/tree: `389c4aab6704f4ec2ca4daa16b5b341abe140e89` / `662f59c4fed285d7801369aa84083400cedea7d3`

Materials given:

- `skills/personal/th-projects/subskills/project-direction/references/launch-gate.md`
- `projects/ai-usage-telemetry/about/heart-and-soul/vision.md`
- `projects/ai-usage-telemetry/about/README.md`, including `CURRENT_STATE`
- `projects/ai-usage-telemetry/docs/launch-gate/parameters.md`
- Seven applicable files under `projects/ai-usage-telemetry/about/craft-and-care/`
- Five applicable files under `projects/ai-usage-telemetry/about/lay-and-land/`
- Five applicable non-review files under `projects/ai-usage-telemetry/about/legends-and-lore/`: README, three evidence annexes, and RFC 0001
- `projects/ai-usage-telemetry/docs/launch-gate/trend.md`, read only for F1
- Named commit/tree metadata

Every repository read was commit-anchored to `389c4aab6704f4ec2ca4daa16b5b341abe140e89`.

Deviations and limitations:

- The two files under `about/legends-and-lore/reviews/0001/` were listed during permitted corpus traversal but not read because review history was explicitly withheld.
- `parameter-review.md`, authoring history, prior reviews, prior reviewer output, the temporary E4 key file, and all personal/session/auth data were not read.
- The referenced shared `spec-format.md` and superskill router were not read because they were outside the strict allowlist. E1 was judged from the instrument’s default binding and the project-specific bindings reproduced in `parameters.md`.
- There is no key in the allowed context, so classifications are recorded only; I did not inspect or infer a sealed key. This makes E4 unresolvable in this administration.
- No network access was used.

Operationalization notes:

- “Chunk” in B1/B2/B4/B5 means independently reviewable project-shape or capability units, with special attention to the separate capabilities mandated for the initial OpenSpec changeset.
- “Major shape commitments” in A5 means RFC 0001 decisions D1–D10.
- For C2, repeated statements used as engineering tests or topology summaries were not treated as competing owners when the corpus explicitly routed semantic ownership to RFC 0001.
- D1 was performed through the fixed front door and corpus without history.
- For E3, unresolved implementation-measured profile values were treated as downstream profile/spec work, not doctrine questions, because the accepted evidence explicitly assigns them there.
- P3–P5 were evaluated against the first capability plus the mandatory same-initial-changeset companion capabilities where the question spans outcomes explicitly excluded from the first capability.
- `[P]` tier drops: none.

## Verdict summary

| Q | Verdict |
|---|---|
| A1 | Met |
| A2 | Not met |
| A3 | Met |
| A4 | Met |
| A5 | Met |
| A6 | Not met |
| B1 | Not met |
| B2 | Met |
| B3 | Met |
| B4 | Not met |
| B5 | Met |
| C1 | Met |
| C2 | Met |
| C3 | Not met |
| C4 | Met |
| C5 | Met |
| C6 | Met |
| D1 | Met |
| D2 | Met |
| D3 | Not met |
| D4 | Met |
| E1 | Met |
| E2 | Met |
| E3 | Met |
| E4 | Unknown(answer key withheld; required comparison cannot be performed) |
| E5 | Met |
| E6 | Met |
| F1 | Unknown(first administration; no prior comparable trend exists) |
| F2 | Not met |
| F3 | Met |
| F4 | Met |
| G1 | No verdict |
| P1 | Met |
| P2 | Met |
| P3 | Met |
| P4 | Met |
| P5 | Met |

## A. Problem and vision

### A1

> **A1 [U]** Does the project name a specific, *lived* human problem — one  
> whose resolution would be observable in a specific person's behavior?  
> *Fails when:* the problem is a category ("agent observability") rather than  
> an experience.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/heart-and-soul/vision.md` — “It is for any developer who wants one durable account of tokens, unique usage requests, and quota state across supported local tools without surrendering private conversation content or account credentials.”
- Same path — “AI Usage Telemetry succeeds when a developer can leave one container running, query a durable and eventually exact history of supported local usage, compare available local quota state without unit ambiguity, and opt into bounded exports…”

Falsification attempt: I tried to reduce the problem to the category “telemetry.” The observable user behavior—leaving one local service running and querying a durable private history—survived that attempt.

### A2

> **A2 [U]** Is the thesis falsifiable — does the project state what evidence  
> would prove it wrong, and when that judgment gets made?  
> *Fails when:* every conceivable outcome can be narrated as success, or the  
> falsifier has no trigger point.

Verdict: `Not met`

Counterexample:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/heart-and-soul/vision.md` — “AI Usage Telemetry succeeds when a developer can leave one container running, query a durable and eventually exact history…”
- The same goal statement defines violations of individual principles but never states an evidence result that falsifies the overall thesis or a lifecycle point when that judgment is made.

Quantified absence sweep: the fixed goal-statement denominator is 1 file; 0/1 states an overall thesis falsifier plus its trigger point.

Falsification attempt: I tested whether the “A violation is” clauses supply the missing falsifier. They define constitutional breaches, not evidence that the product thesis is wrong or when the project must make that judgment.

### A3

> **A3 [U]** Are the exclusion boundaries sharp enough to *reject a plausible  
> near-miss* — could a reviewer, citing only the goal statement, refuse a  
> product that superficially resembles the goal?  
> *Fails when:* the is/is-not list only excludes strawmen nobody would build.  
> *Administer concretely:* judge the pre-written `NEAR_MISSES` from the  
> parameter block, accepting or rejecting each with a citation — never invent  
> softer near-misses on the spot.

Verdict: `Met`

Near-miss decisions:

1. Local AI Workbench Journal — rejected.

   - `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/heart-and-soul/vision.md` — “It is not a prompt, response, conversation, or tool-call auditing system.”
   - Same path — “Prompt and response content values must never be decoded, materialized, logged, retained, or exported.”

2. AI Plan Optimizer — rejected.

   - Same path — “It is not a billing ledger, token-cost calculator, or promise of provider invoice accuracy.”
   - Same path — “A violation is: … adding cost estimation before exact usage accounting works…”

3. Quota Guardian — rejected.

   - Same path — “It is not a cloud-account collector and must not require provider or tool credentials.”
   - Same path — “Credential-bearing auth stores must never be mounted.”

Falsification attempt: Each candidate provides plausible local-AI value and preserves some superficial privacy or telemetry traits. All three are nevertheless rejected by explicit constitutional boundaries, without relying on the shape corpus.

### A4

> **A4 [U]** Is the problem tractable as scoped: does a smallest end-to-end  
> slice exist that exercises the core loop and could be built and honestly  
> evaluated with bounded effort?  
> *Fails when:* the first demonstrable value requires most of the system.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Specify one end-to-end outcome: a qualified, fully synthetic Claude Code 2.1.226 usage record is field-projected without materializing content, normalized, persisted into a disposable SQLite ledger, replayed without a second contribution, and visible through one read-only local usage view.”
- Same path — “This capability spec does not own Codex, quota, real personal mounts, production activation, multi-stream quarantine/reconciliation, low-space and migration recovery, complete health surfaces, container packaging, OTLP/PostgreSQL, dashboards, billing/cost, OpenCode, or host schedulers.”

Falsification attempt: I checked whether meaningful proof required sinks, real mounts, container packaging, or both source adapters. The bounded synthetic record→ledger→view loop tests privacy, durability, and replay without them.

### A5

> **A5 [U]** Are the project's consumers enumerated, and does every major shape  
> commitment trace to a named consumer's need — and every named consumer to at  
> least one commitment that serves it?  
> *Fails when:* a consumer exists only in rhetoric, or a shape element serves  
> no one who was named.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/heart-and-soul/vision.md` — “It is for any developer who wants one durable account…”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “The following decisions are load-bearing. Their principle titles are reproduced exactly so downstream specs can trace back without interpretation.”
- Same path — “D1 | Keep an indefinite, normalized local ledger as the accounting authority. | **1. Local Facts Become User-Owned History**; **3. Accounting Is Eventually Exact**”

Trace check: 10/10 load-bearing decisions D1–D10 cite one or more consumer-facing doctrine principles. The named developer is served directly by D1’s durable history commitment and by the privacy, exactness, failure, normalization, runtime, and simplicity commitments behind the remaining rows.

Falsification attempt: I looked for an orphaned RFC decision and for a named consumer without a serving commitment. The D1–D10 table supplied neither.

### A6

> **A6 [U]** Is the scope achievable with the resources the project actually  
> has — people, attention, money, calendar — stated rather than implied?  
> *Fails when:* the shape silently assumes a team, budget, or cadence the  
> project does not have.

Verdict: `Not met`

Counterexample and absence sweep:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Tze, repository owner.” This identifies the decider, not available implementation capacity.
- Across all 20 fixed project-evidence files—vision, entry/current-state document, parameter block, and 17 applicable shape-corpus files—0/20 states available implementers, attention allocation, budget, or delivery calendar.

Falsification attempt: I treated the five-minute poll interval, target architectures, and review-cycle count as possible “calendar/resources.” They are product or process requirements, not a statement of resources actually available.

## B. Decomposability and sequencing

### B1

> **B1 [U]** Does the shape decompose into chunks each independently  
> understandable, independently acceptable-or-rejectable, and valuable before  
> later chunks exist?  
> *Fails when:* acceptance is practically all-or-nothing, whatever the  
> packaging says.

Verdict: `Not met`

Counterexample:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “every other lifecycle/source/sink/runtime outcome remains a separate capability in the same RFC-required initial changeset”
- Same path — “Tze accepts a changeset.”

The capability packaging is separate, but the mandated initial acceptance unit is one changeset containing every RFC-required outcome. That is practically all-or-nothing at the stated acceptance boundary.

Falsification attempt: I tried to treat “separate capability” as proof of independent acceptance. The authority binding still accepts the changeset, and the companion outcomes may not be deferred beyond it.

### B2

> **B2 [U]** Is chunk ordering explicit and acyclic — does no chunk's  
> acceptance silently presume an unaccepted one?  
> *Fails when:* a chunk's text relies on a sibling that could still be  
> rejected, with no stated fallback.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/README.md` — “A lower-layer document cannot make a higher-layer draft authoritative.”
- Same path — “Heart-and-soul doctrine | **Adopted**”; “RFC 0001 | **Accepted**”; “Lay-and-land topology | **Accepted**”; “Craft-and-care standards | **Adopted**”; “OpenSpec capability specifications | **Absent**”
- Same path — “OpenSpec remains absent until the launch gate passes. After it exists, implementation work must trace from adopted doctrine through an accepted RFC and a signed-off requirement before code is written.”

Falsification attempt: I looked for a cycle in which RFC authority depended on OpenSpec or implementation evidence. The lifecycle and authority text establish doctrine→RFC→topology/standards→OpenSpec→implementation, with lower layers unable to activate higher drafts.

### B3

> **B3 [U]** Does the first buildable slice generate *evidence about the thesis  
> itself*, not just infrastructure?  
> *Fails when:* the slice proves the team can build plumbing but nothing about  
> whether the product idea is right.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “The primary acceptance boundary is one synthetic source record → one durable, content-free, locally queryable contribution.”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/heart-and-soul/vision.md` — “AI Usage Telemetry makes raw local AI-tool usage facts legible and queryable as user-owned history.”

Falsification attempt: I tried to classify SQLite persistence as plumbing only. The slice additionally proves content non-materialization, durable user-owned history, queryability, and replay exactness—the thesis’s core claims.

### B4

> **B4 [U]** Can any single chunk be rejected without collapsing the whole — is  
> partial acceptance a designed state rather than an accident?  
> *Fails when:* rejecting one chunk invalidates digests, references, or  
> premises across the others.

Verdict: `Not met`

Counterexample:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Companion capability specs in the **same initial OpenSpec changeset** own every in-v1 outcome that RFC 0001 requires before implementation or release; excluding them from this one capability must not defer them beyond that changeset.”
- Same path — “Tze accepts a changeset.”

Rejecting any mandatory companion outcome prevents acceptance of the single required initial changeset. No partial-acceptance state or fallback is defined.

Falsification attempt: I checked whether the separate capability granularity allowed the remaining capabilities to be accepted independently. The explicit “same initial changeset” and changeset-level authority close that route.

### B5

> **B5 [U]** Does the sequencing retire risk early — are the assumptions most  
> likely to invalidate the thesis scheduled to be tested in the earliest chunks  
> that can test them?  
> *Fails when:* early chunks are the comfortable ones and every  
> thesis-threatening unknown lives late — or nobody can name the riskiest  
> assumption at all.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “one synthetic source record → one durable, content-free, locally queryable contribution”
- Same path — “Companion capability specs in the same initial OpenSpec changeset own every in-v1 outcome that RFC 0001 requires before implementation or release”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/evidence/0001-source-and-bounds.md` — “No numeric release profile is asserted by this annex because no implementation or measurement harness exists yet. The first OpenSpec changeset must specify the profile schemas and may authorize a test-only, non-production measurement harness under finite candidate bounds.”

Falsification attempt: I looked for comfortable scaffolding ahead of privacy, identity, replay, and unmeasured-profile risk. Privacy/replay are in the first vertical slice, and the remaining activation-gate unknowns are required in that same initial changeset.

## C. Authority discipline

### C1

> **C1 [U]** Does authority flow one way — doctrine → shape → specs →  
> implementation — with each layer citing upward and no lower layer quietly  
> redefining a higher one?  
> *Fails when:* a lower artifact restates a higher rule with drifted meaning,  
> or a summary is treated as the rule.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/README.md` — “Topology and craft-and-care derive authority from that doctrine and RFC; they do not override them.”
- Same path — “After [OpenSpec] exists, implementation work must trace from adopted doctrine through an accepted RFC and a signed-off requirement before code is written.”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “OpenSpec owns testable requirements derived from this RFC.”

Falsification attempt: I compared entry, topology, craft, and RFC statements for a lower-layer override. The routing text consistently gives doctrine/RFC precedence and sends behavior to OpenSpec downstream.

### C2

> **C2 [U]** Is every normative "should" owned by exactly one artifact, with  
> conflicts between artifacts resolved by rule rather than by reading order?  
> *Fails when:* two artifacts answer the same question and a reader must guess  
> which wins; or a rule lives only inside a validator, index, or generated  
> report.  
> *Scope note:* C2 asks whether exactly one owner **exists** (ownership); D2  
> asks whether a reader can **find** it (routing). Evidence for one is not  
> evidence for the other.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/craft-and-care/README.md` — “Detailed event identity, checkpoint, ledger, and delivery semantics belong in RFC 0001.”
- Same path — “OpenSpec is currently absent; normative feature behavior belongs there only after doctrine adoption and RFC acceptance. Deployment topology belongs in `about/lay-and-land/`.”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/lay-and-land/README.md` — “If the map and contract disagree, treat that as documentation drift and resolve the governing decision before implementation.”

Falsification attempt: The same quarantine and privacy language appears in RFC, engineering standards, tests, and topology. I attempted to treat these as multiple semantic owners, but the corpus explicitly assigns design semantics to RFC, feature behavior to OpenSpec, evidence posture to craft, and placement to topology.

### C3

> **C3 [P]** Does the project apply its own epistemics *to itself* — are claims  
> about its own state evidence-backed and labeled ([Observed] / [Inferred] /  
> [Unknown]), with absence of evidence rendered as unknown, never as done?  
> *Fails when:* a status document claims green/zero/complete without a sweep  
> run against the current bytes.

Verdict: `Not met`

Counterexamples:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “**Status:** Reviewed binding; one review pass and one confirming pass completed”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/README.md` — “OpenSpec capability specifications | **Absent**”

Neither state claim carries an epistemic label. The reviewed-binding claim points to explicitly withheld review evidence; the absence claim does not include a current-byte sweep in the allowed evidence.

Falsification attempt: I checked the accepted-shape digests and the entry’s epistemic preamble. They support and explain many shape claims, but they do not label or provide current-byte evidence for these two project-state assertions.

### C4

> **C4 [P]** Are the human decision points enumerated and non-delegable — can a  
> reader determine *from artifacts alone* whether any given decision was  
> actually made by a human?  
> *Fails when:* an agent-authored artifact could pass as an owner act, or a  
> pending decision is indistinguishable from a made one.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Only an explicit owner directive or recorded owner decision performs an owner act. Agents may draft, review, and report gate verdicts; they cannot adopt doctrine, accept an RFC/spec, waive a gate, or expand irreversible scope.”
- Same path — “Tze accepts a changeset.”

Falsification attempt: I tried to find an avenue for this gate record, a review, or an agent-authored draft to perform acceptance. The binding explicitly denies all three.

### C5

> **C5 [U]** Does the shape separate what the project controls from what it  
> assumes of its substrate — external tools, formats, platforms — with each  
> load-bearing assumption recorded alongside a stated posture if it breaks?  
> *Fails when:* an external dependency is treated as guaranteed, or its failure  
> would reshape the project and no artifact admits that.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/evidence/0001-source-and-bounds.md` — “Claude Code 2.1.226 | **Observed**, version-specific session JSONL fields”
- Same path — “OpenCode | Partial local shape only | Not established | Defer until identity, replay/mutation, privacy, and failure semantics are reviewed end to end”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/lay-and-land/components.md` — “**[Unknown] Future tools** may lack a content-free local usage source and are not assumed to fit the adapter contract automatically.”

Falsification attempt: I checked source formats, quota availability, future tools, optional sinks, and architectures for guaranteed-substrate language. Each load-bearing uncertainty receives an observed/inferred/unknown status and a fail-closed, unavailable, deferred, or release-blocked posture.

### C6

> **C6 [U]** Does required authority scale with irreversibility — are  
> hard-to-undo or externally visible actions gated more strongly than routine,  
> revertable ones?  
> *Fails when:* one approval level covers everything, or an irreversible effect  
> is reachable through a routine path.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Agents may draft, review, and report gate verdicts; they cannot adopt doctrine, accept an RFC/spec, waive a gate, or expand irreversible scope.”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “A privacy repair is a special, owner-authorized operation on an isolated backup… The exact repair plan and blast radius require owner approval before mutation.”
- Same path — “Non-secret collector configuration is TOML. It selects enabled canonical source targets, human-facing aliases, polling interval…”

Falsification attempt: I compared routine alias/cadence configuration with specification acceptance, doctrine changes, privacy repair, and irreversible expansion. They do not share one authority level.

## D. Comprehensibility

### D1

> **D1 [U]** Fresh-engineer test: can a capable engineer with no prior contact  
> state the problem, thesis, current lifecycle stage, and next pending decision  
> after bounded reading (≤ 60 min)?  
> *Fails when:* correctness requires archaeology — reading history, resolving  
> contradictory metadata, or knowing which documents are stale.

Verdict: `Met`

Fresh-engineer outcome:

- Problem: developers lack one durable, content-free local account of usage and quota facts.
- Thesis: a narrow local service can normalize supported local events into an authoritative SQLite ledger and optional independent projections.
- Stage: adopted doctrine, accepted RFC/topology, pre-implementation, OpenSpec absent.
- Next decision: administer this gate at a named commit; only a `READY` result removes the objection to beginning the initial OpenSpec changeset.

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/heart-and-soul/vision.md` — “A self-contained, long-running local service can normalize those facts into a durable SQLite ledger…”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/README.md` — “The project is still pre-implementation.”
- Same path — “OpenSpec capability specifications | **Absent** | Launch parameters reviewed; next gate: a `READY` administration at a named commit”

Falsification attempt: I did not read history or reviews. The fixed front door and corpus supplied all four answers without resolving contradictory metadata.

### D2

> **D2 [U]** Task-routing test: from the front door, can they reach the  
> *single* rule governing one concrete task without exhaustive reading?  
> *Administer concretely:* use the tasks fixed in the parameter block  
> (`D2_ROUTINE_TASK`, `D2_AUTHORITY_TASK`, `D2_SEAM_TASK`), chosen before the  
> administration — never by the reviewer or the administering session mid-run.  
> At least one task must cross a seam between two chunks.  
> *Fails when:* the route requires reading the corpus, or two routes give two  
> answers.

Verdict: `Met`

Routes:

1. Routine alias task:

   `about/README.md` → recommended `vision.md` → RFC 0001 Configuration Contract.

   - `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/heart-and-soul/vision.md` — “Account identity that would require credentials is represented only by an explicitly configured local alias.”
   - `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “Non-secret collector configuration is TOML. It selects … human-facing aliases…”

2. Credential-store authority task:

   `about/README.md` → `vision.md`, constitutional exclusion.

   - `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/heart-and-soul/vision.md` — “Credential-bearing auth stores must never be mounted.”
   - `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “Forbidden without an owner-adopted doctrine amendment”

3. Extraction→ledger→PostgreSQL seam task:

   `about/README.md` → `craft-and-care/README.md` or RFC 0001 → Privacy and Cardinality Budget.

   - `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “Four deny-by-default contracts are intentionally separate and ordered: 1. the code-owned extraction registry… 2. the versioned ledger schema… 3. the PostgreSQL projection allowlist… 4. the OTLP attribute/vocabulary policy…”
   - Same path — “Permission at one layer grants no permission at another.”

Falsification attempt: I looked for a second rule that allowed credentials or let one field approval flow through all layers. The routes converge on a single prohibition and a single four-control seam contract.

### D3

> **D3 [U]** Is invented vocabulary minimal and defined-before-use — does each  
> coined term buy clarity ordinary language couldn't?  
> *Fails when:* the default reading path uses a term before defining it, or a  
> coined term shadows an ordinary word's meaning.

Verdict: `Not met`

Counterexample:

- First use in `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “Whether a particular counter is inclusive, exclusive, or repeated on a rate-limit-only update is frozen by the versioned release profile…”
- Definition appears later in the same default-path document — “Every release embeds an immutable, code-owned **release profile**. Its ID and content digest cover…”

“Release profile” is useful vocabulary, but it is used before it is defined.

Falsification attempt: I checked the entry document, vision, and preceding RFC sections for an earlier definition. None appears before the Evidence Baseline use.

### D4

> **D4 [U]** Do the entry/summary documents make no claim their sources don't —  
> is simplification confined to presentation, never meaning?  
> *Fails when:* an overview asserts something stronger, softer, or fresher than  
> its owning source.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/README.md` — “The documents describe an adopted pre-implementation shape, not a claim that the service already exists or that its release evidence is complete.”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/lay-and-land/components.md` — “No application package or runtime image exists yet.”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/README.md` — “Source/resource profiles still require their downstream evidence gates before real mounts, ingestion, exports, or a release claim are enabled.”

Falsification attempt: I specifically tested whether “Accepted” in the entry could be mistaken for implemented or release-ready. Both the entry and owning sources explicitly prevent that stronger reading.

## E. Readiness to author specifications

### E1

> **E1 [U]** Is it defined what a specification *is* here — form, home,  
> granularity, acceptance authority, and change process — before the first one  
> is written?  
> *Fails when:* the first spec author would have to invent the medium while  
> writing the message.  
> *Record sub-verdicts:* form, home, granularity, acceptance authority, change  
> process — five answers, not one. E1 is Met only when all five are.  
> *Default binding:* a th-projects repo answers form/home/change-process with  
> [`references/spec-format.md`](../../../references/spec-format.md) and  
> `openspec/`; granularity and acceptance authority are still project answers.

Overall verdict: `Met`

Five separate sub-verdicts:

1. Form — `Met`

   - `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Normative MUST/SHALL requirements with stable ID, Source, Scope, and immediately adjacent WHEN/THEN scenarios per the shared spec format”
   - Falsification attempt: the external format reference was unavailable, but the project binding itself reproduces the required authoring form.

2. Home — `Met`

   - Same path — “Accepted specs in `openspec/specs/`; active deltas and rationale in `openspec/changes/`”
   - Falsification attempt: I checked whether pre-gate directory absence made the home undefined. The location is bound even though its creation is correctly deferred.

3. Granularity — `Met`

   - Same path — “One falsifiable vertical outcome with one primary acceptance boundary”
   - Falsification attempt: I checked for a vague subsystem-sized unit. The first capability is explicitly record→ledger→view, and other outcomes are separate capabilities.

4. Acceptance authority — `Met`

   - Same path — “Tze accepts a changeset.”
   - Same path — “the recorded conditional direction activates only after a `READY` gate, mechanical trace checks, at least four sequential th-projects/th-engineering improvement cycles with fixes between, and no unresolved blocker”
   - Falsification attempt: agents cannot accept or waive this authority.

5. Change process — `Met`

   - Same path — “Amend through project-feature-request/spec delta; propagate accepted shape changes to affected specs in the same change and use project-review reconciliation as a backstop”
   - Falsification attempt: the process defines both per-change propagation and a reconciliation backstop.

### E2

> **E2 [U]** Is the first specification identified, with every prerequisite  
> either satisfied or explicitly waived on the record?  
> *Fails when:* "what comes first" is answered differently by different  
> documents, or prerequisites are discovered rather than listed.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “`FIRST_SPEC_CANDIDATE` is **Synthetic-to-SQLite Usage Spine**”
- Same path contains six listed prerequisites, five marked satisfied and one explicit waiver.
- Same path — “OpenSpec directory | Explicitly waived by the launch-gate protocol and Owner Decision 0001: pre-gate absence is required…”

Falsification attempt: I checked the entry’s “OpenSpec absent” state as a possible undiscovered blocker. It is the explicitly documented waiver, not a contradiction.

### E3

> **E3 [U]** Would authoring the first spec force reopening any doctrine- or  
> shape-level question? Enumerate them.  
> *The sharpest single gate:* hand the reviewer the shape corpus and the first  
> spec's charter, and ask what they would have to reopen. An empty list —  
> genuinely arrived at, not asserted — is the readiness signal.  
> *Fails when:* the list is non-empty; "ready" is then false regardless of  
> every other verdict.  
> *Credibility protocol:* the reviewer first enumerates the first spec's  
> central concepts and obligations, then traces each to the shape artifact that  
> would govern it. An empty reopen-list **without this trace table** is  
> recorded as Unknown, not Met — a shallow reading also produces an empty list.

Verdict: `Met`

Central concepts and obligations:

- Qualified synthetic Claude 2.1.226 record
- Exact field-projecting, non-materializing parser boundary
- Claude record discrimination, paths, identity, context, and token mapping
- Normalized event identity, fingerprint, source time, attribution, and amounts
- Transactional SQLite persistence and durable local authority
- Exactly-once contribution under replay
- Stable read-only `usage_events` view
- Explicit separation from real mounts, Codex, quota, sinks, runtime packaging, and recovery companions
- Profile-schema/evidence gates for currently unmeasured numeric limits

Trace table:

| Central concept/obligation | Governing shape evidence |
|---|---|
| Synthetic-only input | `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/evidence/0001-synthetic-vectors.md` — “Every fixture is hand-authored or generated from a local mock; real session, credential, or application-state bytes are prohibited.” |
| Claude 2.1.226 admitted shape | `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/evidence/0001-source-and-bounds.md` — “a fully synthetic loopback-only capture from Claude Code 2.1.226 proved co-occurring string paths…” |
| Non-materializing parser | `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “A value at a content-bearing or unregistered path is syntax-scanned and skipped directly from the input buffer. It is never decoded, materialized…” |
| Claude discriminator and paths | Same path — “Usage-bearing records have the registered value `assistant`” and “Permitted paths and types…” |
| Native identity | Same path — “Native usage identity: `(sessionId, requestId)`. Both are required.” |
| Token mapping | Same path — “`input_tokens` -> `input_uncached`, `cache_creation_input_tokens` -> `input_cache_write`, `cache_read_input_tokens` -> `input_cache_read`, and `output_tokens` -> `output_unclassified`” |
| Event identity/fingerprint | Same path — “The immutable tuple `(collector_namespace, ledger_namespace, adapter_schema_id, source_namespace, fact_kind, native_identity)`” and “SHA-256 of RFC 8785 canonical JSON…” |
| Durable SQLite authority | Same path — “SQLite under `/data` is the durable accounting authority.” |
| Atomic contribution | Same path — “For each consumed record, one SQLite transaction coordinates…” |
| Replay behavior | Same path — “A same-identity, same-fingerprint duplicate advances the source cursor without changing history, aggregates, or sink obligations.” |
| Local query view | Same path — “SQLite exposes stable, read-only query views named `usage_events`…” |
| Companion capability boundary | `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Companion capability specs in the same initial OpenSpec changeset own every in-v1 outcome that RFC 0001 requires…” |
| Unmeasured numeric profiles | `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/evidence/0001-source-and-bounds.md` — “Exact numeric values belong in those reviewed profiles because they depend on the implementation and supported workload” |

E3 reopen-list: empty.

Falsification attempt: I tried to force reopening of content handling, Claude identity, token arithmetic, replay semantics, transaction boundaries, view ownership, and exact parser limits. The first six are already shape decisions. Exact numeric profiles are explicitly assigned to reviewed downstream profiles and executable evidence, so choosing them does not require changing doctrine or RFC shape.

### E4

> **E4 [U]** Is the shape/spec boundary crisp enough that an author knows which  
> side any given sentence falls on — without asking?  
> *Administer concretely:* the reviewer classifies the five pre-written  
> `E4_CANDIDATES` from the parameter block; disagreement with the project's own  
> routing is a fail. Candidates are written before the administration, like  
> `NEAR_MISSES` — a reviewer who picks their own examples picks easy ones.

Verdict: `Unknown(answer key withheld; required comparison cannot be performed)`

Independent classifications, fixed before any comparison:

| Candidate | Classification | Reason |
|---|---|---|
| “Accepted local facts remain in SQLite indefinitely; optional sinks never become the history authority.” | Shape | Constitutional retention and authority allocation; already owned by doctrine/RFC. |
| “Content and credentials may not be decoded, materialized, logged, retained, or exported.” | Shape | Non-negotiable privacy boundary requiring doctrine amendment to change. |
| “Replaying an already accepted source event must not create another normalized token or request contribution.” | Specification | Directly falsifiable observable behavior suitable for WHEN/THEN acceptance scenarios, derived from the shape’s exact-accounting rule. |
| “An unknown or malformed complete record must quarantine only its affected stream while healthy streams continue and aggregate health remains degraded.” | Specification | Observable failure-isolation scenario derived from the RFC; detailed enough to be test acceptance. |
| “The collector is one non-root long-running container with a five-minute default poll and no inbound port.” | Shape | Runtime topology and trust-boundary commitment. |

Material limitation: There is no key in the allowed context, so classifications are recorded only; I did not inspect or infer a sealed key.

Evidence needed to settle: disclose the committed nonce and answer key only after these classifications are fixed, verify the key against SHA-256 `4f38f814de67238ddc4d84518149ebcc7344b148a35c4e15ce9bb38fe36ac610`, and compare all five classifications.

Falsification attempt: I independently separated constitutional/architectural commitments from concrete observable scenarios. Without the withheld project routing, the instrument forbids converting that judgment into `Met` or `Not met`.

### E5

> **E5 [U]** Do acceptance criteria exist for a spec itself — how one will be  
> judged complete, testable, and faithful to the shape above it?  
> *Fails when:* spec acceptance would be a vibe check by whoever reviews it.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Normative MUST/SHALL requirements with stable ID, Source, Scope, and immediately adjacent WHEN/THEN scenarios”
- Same path — “mechanical trace checks, at least four sequential th-projects/th-engineering improvement cycles with fixes between, and no unresolved blocker”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/craft-and-care/review-and-documentation.md` — “the governing doctrine principle, RFC section, OpenSpec requirement when one exists, and affected topology/standard”

Falsification attempt: I checked whether owner acceptance could occur from prose preference alone. Stable requirements, executable scenarios, upward trace, mechanical checks, sequential quality cycles, and blocker closure constrain it.

### E6

> **E6 [U]** Is there a defined propagation path for a shape change *after*  
> specs exist — how affected specs are detected, who amends them, and how the  
> interim disagreement is surfaced rather than hidden?  
> *Fails when:* the first post-spec shape amendment would create silent  
> contradictions between layers.  
> *Default binding:* th-projects answers this with per-change spec sync  
> (feature-request amendment mode) plus episodic reconciliation as backstop;  
> Met still requires the project to have adopted it, not merely inherited it.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Amend through project-feature-request/spec delta; propagate accepted shape changes to affected specs in the same change and use project-review reconciliation as a backstop”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/craft-and-care/review-and-documentation.md` — “A doctrine change follows the owner-adoption process and reconciles every affected downstream artifact before implementation continues.”
- Same path — “‘Update the docs later’ is not an acceptable disposition…”

Falsification attempt: I looked for a period in which implementation could continue with knowingly conflicting shape and spec. The adopted same-change and stop/reconcile rules prohibit it.

## F. Process health and convergence

### F1

> **F1 [U]** Is the improvement cycle *converging* — are successive review  
> rounds finding fewer and less severe defects, with a declared stop condition  
> other than exhaustion?  
> *Requires the trend log (§5).*  
> *Fails when:* each round's fixes mint the next round's findings, or no one  
> can state what would end the cycle.

Verdict: `Unknown(first administration; no prior comparable trend exists)`

First-run handling:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/trend.md` contains only the heading and empty table header; administration denominator is 0.
- The instrument declares a stop condition—F1 convergence plus the §4 rule—but no successive administrations exist from which to determine direction.

Evidence needed to settle: at least one later full administration against the unchanged v1.0 questions, recorded alongside this administration, with comparable Not-met, Unknown, Reopened, and new-finding columns.

Falsification attempt: I did not infer convergence from parameter-review passes or review history; F1 exclusively requires the trend log.

### F2

> **F2 [U]** Is the governance corpus proportionate to what it protects — does  
> each artifact have an owner and a retirement path, and does repairing a  
> defect not routinely create new artifacts?  
> *Fails when:* the corpus grows monotonically; defect → new report → new  
> validator → new defect.  
> *Operational proxies:* artifact-count trend across cycles; the ratio of  
> normative artifacts to meta-artifacts (reports, validators, indexes) — meta  
> should not outgrow normative; a sweep for artifacts no reading route reaches.

Verdict: `Not met`

Quantified absence sweep:

- Exact prefix denominator: 17 applicable files across the three fixed shape-corpus prefixes.
- Explicit author/owner marker: 1/17 (`RFC 0001` names `Author: Codex`).
- Artifact-level retirement path or condition: 0/17.
- Therefore 0/17 satisfies both “owner” and “retirement path” explicitly.

Concrete example:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/lay-and-land/assets/README.md` — “If a later diagram becomes too dense for Mermaid, store its editable `.excalidraw` source and rendered SVG here.” It defines future growth rules but no artifact owner or retirement condition.

Falsification attempt: I searched compatibility, supersession, deletion, and documentation rules for an artifact-retirement contract. They govern persisted data or normative evolution, not retirement of every governance artifact.

### F3

> **F3 [U]** Can the owner make each acceptance decision from a bounded packet  
> in one sitting, without archaeology?  
> *Fails when:* the honest answer to "what am I binding?" requires reading  
> history or trusting a summary the packet itself calls non-authoritative.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/craft-and-care/review-and-documentation.md` — “Before requesting review, the author records: the governing doctrine principle, RFC section, OpenSpec requirement when one exists, and affected topology/standard; … the exact commands or harnesses run…; … every document or contract changed in the same patch.”
- Same path — “Each round records reviewer, date, exact draft revision, section-scoped findings, author response, evidence, and disposition.”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Tze accepts a changeset.”

Falsification attempt: I checked whether the owner must reconstruct scope and findings from conversation or history. The adopted packet requirements bind exact revision, upward authority, evidence, changed contracts, findings, and dispositions.

### F4

> **F4 [U]** If all improvement stopped today, would the corpus be safe to  
> abandon — no artifact left misstating the current state to a future reader?  
> *Fails when:* any stale claim, superseded offering, or dead route is  
> reachable from a default reading path without a banner.

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/README.md` — “The project is still pre-implementation.”
- Same path — “OpenSpec capability specifications | **Absent**”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/lay-and-land/README.md` — “They remain **[Inferred]** pre-implementation maps and must be reconciled when implementation evidence exists.”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/README.md` — “Source/resource profiles still require their downstream evidence gates…”

Falsification attempt: I looked for reachable claims that the service, release profiles, application package, container image, or specs already exist. Default-path documents consistently banner these as absent, inferred, or downstream-gated.

## G. Gate self-scrutiny

### G1

> **G1 [U]** Completeness critic — the reviewer answers: *what readiness  
> dimension could this project fail that no question above would catch?*  
> Proposed missing questions go into the record and are considered for  
> amendment of this instrument (§9).  
> *G1 yields no Met/Not-met verdict; an administration that skips it is  
> incomplete and cannot support a gate decision.*

No verdict.

Proposed missing question:

> **Evidence-oracle readiness:** Are the test oracles required to accept the first specification reproducible and available at specification time, without requiring forbidden production data or an implementation choice that the spec is supposed to govern?  
> *Fails when:* a requirement is linguistically testable but its expected result cannot be determined from an approved synthetic fixture, model, or independently reproducible oracle.

Why existing questions may miss it: A4 tests bounded tractability and E5 tests whether criteria are expressed as testable, but neither directly tests whether the oracle needed to decide those criteria exists independently of implementation or forbidden real data.

Falsification attempt: This project has an unusually strong synthetic-vector inventory, so the proposed question would likely pass here. It still covers a general readiness defect not directly isolated elsewhere.

## Goal-derived questions

### P1

> **P1 [G]** Does the first-spec candidate preserve a durable local account of  
> accepted facts rather than making an optional sink the effective source of  
> truth?  
> *Fails when:* a sink outage, endpoint change, or projection policy can erase,  
> replace, or redefine the user's history.  
> *Doctrine basis:* `vision.md`, **1. Local Facts Become User-Owned History.**

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “persisted into a disposable SQLite ledger”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “SQLite under `/data` is the durable accounting authority.”
- Same path — “The indefinite local ledger is the source for catch-up and repair.”

Falsification attempt: I disabled or failed both optional sinks conceptually. The first capability has no sink dependency, and retained local history remains authoritative.

### P2

> **P2 [G]** Does the candidate prohibit content and credentials across source  
> collection, diagnostics, persistence, and export?  
> *Fails when:* any supported outcome requires decoding, retaining, or emitting  
> them, including through a convenient error or enrichment path.  
> *Doctrine basis:* `vision.md`, **2. Content and Credentials Stay Outside.**

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “field-projected without materializing content”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/craft-and-care/security-and-privacy.md` — “Errors … do not quote the rejected record, field value, raw parser exception, or content-derived identity.”
- Same path — “Adapters, the ledger, diagnostics, and sinks must never collect or forward” prompt/response content or credentials.

Falsification attempt: I checked error paths, fingerprints, diagnostics, SQLite, PostgreSQL, OTLP, fixtures, and credential-assisted identity. Each is deny-by-default.

### P3

> **P3 [G]** Does the candidate make accounting eventually exact for every  
> supported observable fact across rescans, crashes, and retries while refusing  
> to claim unknowable pre-discovery coverage?  
> *Fails when:* a supported fact can be silently lost or counted twice, or  
> unknown coverage is narrated as reconciled history.  
> *Doctrine basis:* `vision.md`, **3. Accounting Is Eventually Exact.**

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “replayed without a second contribution”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “A crash or database error rolls the whole transaction back, so the cursor can never outrun the facts and delivery state it represents.”
- Same path — “Coverage before source discovery is `coverage_unknown`”
- Same path — “A same-identity, same-fingerprint duplicate … [does not change] history, aggregates, or sink obligations.”

Falsification attempt: I tested replay, transaction interruption, late discovery, and sink retry. Stable identity plus transactional persistence prevents double contribution or silent loss, while pre-discovery coverage remains explicitly unknown.

### P4

> **P4 [G]** Does the candidate make source, storage, and sink failure explicit  
> and scoped without masking degradation or stopping unrelated healthy paths?  
> *Fails when:* aggregate health appears successful while a stream is held/  
> stale, or one failed boundary blocks unrelated collection.  
> *Doctrine basis:* `vision.md`, **4. Partial Failure Is Explicit.**

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — “Companion capability specs in the same initial OpenSpec changeset own every in-v1 outcome that RFC 0001 requires”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/legends-and-lore/rfcs/0001-adapter-ledger-and-sink-contract.md` — “Quarantine is source-scoped. Other source streams, source families, and sinks continue.”
- Same path — “`overall_state=healthy` only when every enabled component is healthy/current under its own contract; it cannot mask a degraded source or sink.”
- Same path — “OTLP and PostgreSQL are separate, independently optional consumers of the ledger.”

Falsification attempt: The first capability excludes full multi-stream/sink behavior, so I checked whether that silently deferred the doctrine. The binding requires separate companion capabilities in the same initial changeset, and the governing behavior is already explicit and non-masking.

### P5

> **P5 [G]** Does the candidate preserve the narrow portable runtime boundary?  
> *Fails when:* it requires broad/writable source mounts, an auth store, host  
> scheduler, inbound API, elevated runtime, or architecture-specific semantics.  
> *Doctrine basis:* `vision.md`, **6. The Runtime Boundary Is Portable and Narrow.**

Verdict: `Met`

Evidence:

- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/about/heart-and-soul/vision.md` — “only explicit, resolved, read-only leaf mounts”
- Same path — “The container runs non-root, exposes no inbound API or port, and behaves the same on the supported amd64 and arm64 targets.”
- `389c4aab6704f4ec2ca4daa16b5b341abe140e89:projects/ai-usage-telemetry/docs/launch-gate/parameters.md` — the first capability excludes real personal mounts, production activation, container packaging, and host schedulers; none is introduced as a shortcut.

Falsification attempt: I checked every forbidden route named by P5. The first slice neither requires one nor overrides the inherited runtime contract; mandatory companion capability coverage remains in the same changeset.

## E1 sub-verdicts

Form `Met` / home `Met` / granularity `Met` / acceptance authority `Met` / change process `Met`

## E3 reopen-list

Empty, supported by the central-concept trace table above.

## Unknowns and what would settle them

- E4 — `Unknown(answer key withheld; required comparison cannot be performed)`. Settle by disclosing and digest-verifying the committed nonce/key after these classifications are fixed, then comparing all five.
- F1 — `Unknown(first administration; no prior comparable trend exists)`. Settle with a later comparable full administration recorded in `trend.md`.

## Reviewer’s falsification notes

The most consequential successful falsification attempts were:

- A2: principle-violation clauses do not provide an overall thesis falsifier or decision trigger.
- A6: product cadence and owner identity do not state actual project resources.
- B1/B4: separate capability files do not create partial acceptance when every required capability must enter one owner-accepted initial changeset.
- C3: current-state assertions are not consistently epistemically labeled or accompanied by current-byte evidence.
- D3: “release profile” appears on the default path before its definition.
- F2: the complete 17-file corpus has no per-artifact retirement contract.
- E4: comparison cannot be invented when the committed key is withheld.

The principal failed falsification attempts were:

- I could not make the first slice depend on sinks, real mounts, container packaging, or Codex.
- I could not find a lower-layer authority override.
- I could not find a credential, content, permissive parser, silent-loss, or masked-failure escape hatch.
- I could not find a doctrine/shape question that the first capability would have to reopen.
- I could not find a default-path claim that the implementation or release evidence already exists.

## G1 proposed missing questions

Evidence-oracle readiness, as stated under G1.

## Proposed gate verdict per §4

`NOT READY`

Reasons:

- E4 is not `Met`; §4 requires every E question to be `Met`.
- A2, A6, B1, B4, C3, and D3 are `Not met`, violating §4’s requirement of no `Not met` anywhere in A–D.
- F1 is first-run `Unknown`, not evidence of divergence, but it cannot cure the blocking E and A–D findings.
- `READY-WITH-DEFERRALS` is owner-only and is not proposed by this reviewer.

No files were created or modified.

---

## Administrator E4 comparison and finalization

This section was added by the administrator only after the fresh reviewer's
five E4 classifications above were fixed. It does not alter the verbatim
reviewer packet or any of its other verdicts.

### Commitment verification and key reveal

The parameter block at commit
`389c4aab6704f4ec2ca4daa16b5b341abe140e89` committed to SHA-256
`4f38f814de67238ddc4d84518149ebcc7344b148a35c4e15ce9bb38fe36ac610`.
The administrator computed the same digest over the revealed key file:

`4f38f814de67238ddc4d84518149ebcc7344b148a35c4e15ce9bb38fe36ac610`

Digest verification: **match**.

Revealed nonce:

`4616de7a1cbf7f9bf0f78423d23664dccd73305c251db66a5399d1f7743142aa`

Revealed answer key:

| E4 candidate | Committed project routing |
|---|---|
| 1. Accepted local facts remain in SQLite indefinitely; optional sinks never become the history authority. | Shape: doctrine/RFC invariant |
| 2. Content and credentials may not be decoded, materialized, logged, retained, or exported. | Shape: constitutional privacy boundary |
| 3. Replaying an already accepted source event must not create another normalized token or request contribution. | Spec: testable capability behavior derived from exactness doctrine |
| 4. An unknown or malformed complete record must quarantine only its affected stream while healthy streams continue and aggregate health remains degraded. | Spec: observable failure behavior derived from partial-failure doctrine |
| 5. The collector is one non-root long-running container with a five-minute default poll and no inbound port. | Borderline: shape owns the narrow runtime/no-inbound commitment; specs own observable startup, timing, privilege, and network scenarios |

### Classification comparison

| Candidate | Reviewer classification fixed before reveal | Committed routing | Result |
|---|---|---|---|
| 1 | Shape | Shape | Match |
| 2 | Shape | Shape | Match |
| 3 | Specification | Spec | Match |
| 4 | Specification | Spec | Match |
| 5 | Shape | Borderline | **Mismatch** |

Final E4 verdict: `Not met`.

The mismatch on candidate 5 replaces the reviewer's pre-reveal
`Unknown(answer key withheld; required comparison cannot be performed)`.
No other reviewer verdict is changed or softened.

### Final administration result

- `Not met`: 8 — A2, A6, B1, B4, C3, D3, E4, F2.
- `Unknown`: 1 — F1.
- `Deferred`: 0.
- `Reopened`: 0.
- New findings versus prior administration: 8.

Final gate verdict per instrument v1.0 §4: `NOT READY`.

E4 is not `Met`, and A2, A6, B1, B4, C3, and D3 remain `Not met` in
A–D. F1 is a first-run `Unknown`, not evidence of divergence, and does not
change the blocking result.
